- [x] Certain composants ont une option "retournée" changer en "Retourner". Et étendre cette option à tous les composants. Proposer Sur l'axe horizontal et sur l'axe vertical. → fait : retournement H/V (miroir) pour tout composant via 2 boutons dans l'inspecteur (champs flipH/flipV du modèle, transform CSS).
- [x] Dans la zone d'aide, pour les composants actifs, ajouter "Clic droit pour déplacer." → fait : ligne ajoutée à l'aide quand le composant est interactif.
- [x] Déplacer les boutons de trie de la bibliothèque en dessous du mot Composants. → fait : titre « Composants » seul, boutons AZ/🗂 sur la ligne en dessous.
- [x] half, mini et full pour la platine d'essai doivent être en français. → fait : affichés Mini / Moyenne / Grande (valeurs internes inchangées).
- [x] Les composants sur la platine d'essai doivent bouger avec elle comme un groupe. De même que les composants enfiché les un dans les autres par exemple une pico pi sur une grove. → fait : déplacer un support entraîne tout ce qui est enfiché dedans (fils auto, transitif).
- [x] une aide doit apparaitre pour l'edition de fils. → fait : aide affichée sous les propriétés quand un fil est sélectionné (croix/CTRL/double-clic).
- [x] Rajouter une fonction de sauvegarde de fichier et faire un format de fichier zippé qui contient le dessin et le code extension ".projix" → fait : format ZIP .projix (kablix.json + diagram.json + dossier code/), commandes/boutons 💾 Enregistrer et 📂 Ouvrir, extraction du code optionnelle.
- [x] Modifie le format des composants pour qu'il soit entièrement compatible avec celui de wokwi (si pas de problème de licence ou de coût). → fait : composants intégrés = éléments @wokwi/elements (déjà mêmes types/broches) + import/export du format projet Wokwi (diagram.json) via 2 commandes. Aucun souci de licence (format ouvert, éléments MIT). Limites : flip/coudes de fils et composants perso non convertis.
- [x] Importe tous les composants wokwi → fait (liste choisie, 13 composants @wokwi/elements ajoutés) : afficheurs LCD 16×2 / 20×4, OLED SSD1306, NeoPixel / matrice / anneau (visuels) ; bouton poussoir 6 mm (interactif) ; capteurs analogiques NTC température, gaz MQ, pouls ; capteurs numériques flamme, son. Importables/exportables aussi en diagram.json. (Le reste du catalogue Wokwi — ili9341, mpu6050, dht22, hc-sr04, keypad, encodeur… — ajoutable à la demande.)
- [x] Changer numerotage de version pour calver AAAA.MM.N° ex : 2026.6.10 → fait : version passée à 2026.6.0.
- [x] on ne voit pas le bas de la page. Fais en sorte que la partie "envoyer un microcontroleur" soit le bas de la zone visible. → fait : mise en page pleine hauteur, le canvas absorbe l'espace et le moniteur série reste en bas.
- [x] Les pattes de la led c'est A K pas A C. → fait : cathode affichée « K » sur les LED (identifiant interne 'C' conservé pour la simulation).
- [x] Change le zoom pour un ctrl + molette → fait : zoom réservé à Ctrl + molette.
- [x] Les boutons de simulation, démarrer, arréter, pause, pas à pas et vitesse doivent apparaitre sur le canva ; seulement des icônes, le texte dans une bulle → fait : barre flottante en haut-gauche du canvas, icônes seules + info-bulle.
- [x] Pour un composant comme le bouton poussoir le cablage interne doit apparaitre au dessus du composant clairement visible lors d'un clic droit sur le composant. Pareil pour un ci ou les portes logique internes apparaitraient dessus au clic droit. → fait (selon précision : à la SÉLECTION, pas au clic droit) : câblage interne superposé (tracé noir sur fond blanc translucide) quand le composant est sélectionné/posé. Schémas : bouton poussoir, LED, résistance, buzzer (extensible aux CI/portes logiques quand ils existeront).
- [x] On ne traduit plus l'aide pour l'instant on le fera à la fin. Fait seulement la version français et elle doit être en ligne et locale. Elle doit contenir une aide sommaire. → fait : commande « Kablix : Aide » (webview FR locale + lien en ligne GitHub).
- [ ] (partiel) Changer l'image ascii art de l'interface par une copie d'écran avec du texte par dessus. → partiel : ASCII art remplacé par une image + légende. Vraie capture annotée à fournir (pas d'outil de capture/annotation côté agent) — il n'y a que des logos dans images/.
- [x] compléter l'aide avec un pas à pas. Pour arduino (extension "Arduino-VsCode-IDE") et pico pi python (extension "Raspberry pi pico"). → fait : deux tutoriels pas à pas dans la page d'aide.


- [x] Un glisser poser permet d'ajouter un composant directement la où on veut. → fait : glisser un bouton de la palette dépose le composant à l'endroit du lâcher (le clic seul le pose toujours au centre).
- [x] En dessous de la fenêtre propriété ajouter une zone d'aide. La remplir par "+ ou - pour faire tourner le composant" quand un composant est sélectionné. → fait : encart d'aide en bas de l'inspecteur quand un composant est sélectionné.
- [x] Permettre le zoom dezoom molette dans la zone de cablage. → fait : zoom molette centré sur le curseur (calque transformable), badge « ⟳ % » cliquable pour réinitialiser la vue.
- [x] Corriger l'export svg qui est tout crado - Texte de l'arduino géant, fils coupés, composant manquant ou déplacé. → fait : chaque composant forcé à sa taille d'affichage (width/height + viewBox) donc broches alignées sur les fils ; cadrage englobant composants + fils + coudes avec marge ; repli rectangle étiqueté si pas de SVG.
- [x] Proposer une mise à jour des différentes librairies (quand elles existent) en précisant que ça peut casser l'appli et que dans ce cas il faut fait une demande sur le github. → fait : commande « Kablix : Vérifier les mises à jour des bibliothèques » (+ réglage opt-in `kablix.checkUpdatesOnStartup`), interroge npm, avertit que la mise à jour peut casser et renvoie vers les issues GitHub.
- [x] Ajuster la barre avec le nom au texte intérieur (le plus grands des 2). La déplacer en cas de rotation du composant → fait : bandeau large d'au moins le composant ou du nom (le plus grand), recalé au-dessus de la boîte englobante quand le composant est tourné.
- [x] Le point blanc n'est pas visible il faut une couleur foncée ou plutot une croix (X) ombrée blanche avec ombre noire plus ou moins transparente. → fait : poignée de coude des fils transformée en croix X blanche avec halo noir, visible sur tout fond.
- [x] La zone de nom doit disparaitre complètement lorsque les composants ne sont pas sélectionné. Elle peut apparaitre si le composant est sélectionné. Mais le vrai problème est que pour les composants actifs (comme le BP) on ne peut pas les déplacer sans cette zone parce qu'on ne peut pas les attraper. il faudrait que la zone non active permette de le attraper ou bien qu'on puisse les déplacer avexc un clic droit. → fait : bandeau visible à la sélection (ou bouton 🏷 pour tout afficher) + déplacement au clic droit pour tous les composants.
- [x] Prévoir un mode débogage simple (pas à pas et lecture des variables) en c et python → fait : pause/reprise/ralenti, pas à pas, panneau Variables, points d'arrêt depuis la gouttière VS Code et surlignage de la ligne en pause. C via DWARF (avr-objdump), Python via instrumentation du source (préambule __kx + raw-paste). Détails dans docs/DEBOGAGE.md.
- [x] Les fils doivent passer par dessus la carte arduino ou la carte pico pi.
- [x] Le nom au dessus des composants doit pouvoir être masquable (bouton toggle dans le menu) mais la zone pour saisir et déplacer le composant reste.
- [x] Feuille de dessin vide au démarrage.
- [x] Ascenceur pour la bibliothèque des composants.
- [x] Réorganise la bibliothèque. 2 façons possibles, ordre alphabétique, catégorie et en haut une zone avec les derniers utilisés (10 max)
- [x] Rajouter une platine d'essai multi tailles elle doit mettre en surbrillance les broches connectées lors de la pose d'un nouveau composant.

