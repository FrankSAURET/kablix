# Backlog (reste à faire / suivi)
1. ✅ ***Revoir svg pico pi et pico pi w*** → fait en v2026.6.30 : `<kablix-pico-board>` reprend le dessin `parts/picow-module` (Pico W, deux rangées verticales au pas de 10 px), LED GP25 conservée. Reste à peaufiner le placement de la LED (vérif visuelle).
2. ✅ Si on inverse le cablage du + et de la masse des potentiometre leur variation doit s'inverser. il n'ont pas de power sur leurs pattes et donc ils ne changent pas la couleur des fils en rouge ou noir → fait en v2026.6.29 : `potBindings` détecte VCC↔GND permutés (champ `inverted`) → lecture inversée en simulation ; les broches du potentiomètre ne sont déjà plus colorées comme de l'alimentation (depuis v2026.6.28).
3. ✅ fournir mega, nano et mini → fait en v2026.6.18 : Nano / Pro Mini / Mega 2560 (+ Pico W) ajoutés au sélecteur.
4. ✅ Dans toutes les diode al cathode c'est K → fait en v2026.6.29 : la cathode est notée « K » sur la LED (C → K) et sur la barre de LED (C1..C10 → K1..K10) ; identifiants internes inchangés.
5. ✅ Rajout shéma interne barre de leds → fait en v2026.6.29 : schéma interne de la barre de LED (10 diodes A→K) ajouté (`internal-wiring.mts`), visible au bouton ☢.
6. ✅ L' afficheur 7 seg ne marche pas. Tu lui rajoute une propriété K ou A commune, et un schéma interne qui varie selon le cas → fait en v2026.6.29 : **cause = mauvaise broche commune** (le modèle 1 chiffre n'a pas de `DIG1` mais `COM.1`/`COM.2`) → corrigé ; nouvelle propriété **« Broche commune »** (cathode/anode) qui inverse la logique d'allumage ; schéma interne en étoile de 8 diodes vers le commun, sens selon cathode/anode.
7. ⏳ reduire la largeur du curseur du potentiometre à glissiere à la largeur du corp → différé en v2026.6.29 : le curseur appartient au shadow DOM de l'élément `@wokwi/elements` (slide-potentiometer) ; le retoucher sans vérification visuelle (impossible côté agent) risquerait de casser le rendu. À reprendre avec une capture du composant.
8. ✅ Les fils doivent toujours passer par dessus les composants → fait en v2026.6.29 : le calque des fils passe en `z-index: 5` (au-dessus des composants z=3 et du câblage interne z=4) ; le SVG restant `pointer-events:none`, les pastilles de broche dessous restent cliquables.
9. ✅ Si on clique sur l'extrémité d'un fil on doit pouvoir le déplacer → fait en v2026.6.29 : un fil sélectionné affiche une poignée losange à chaque extrémité ; la glisser sur une autre broche (rayon d'accrochage 14 px) reconnecte le fil.
10. ✅ Composant impossible à modifier dans svg. Par exemple, Le groupe svg136 est impossible à modifier parce qu’il contient un <svg> imbriqué au lieu d’un <g>. → fait en v2026.6.29 : l'export SVG ne produit plus de `<svg>` imbriqué ; chaque composant est un `<g transform="translate scale">` (éditable comme un groupe dans Inkscape), avec le CSS du shadow DOM réinjecté mais **scopé** à l'id du groupe (plus de fuite de styles entre composants).
11. ✅ l'import wokwi ne respecte pas le cablage "aucun angle" ou la rotation des boards → partiel en v2026.6.29 : la **rotation** des composants est déjà conservée à l'aller-retour (`rotate`↔`rotation`, depuis l'écriture de `wokwi.mts`) ; le **routage orthogonal** des fils Wokwi (mini-langage de chemin `h/v/*` du champ `connections[3]`) n'est pas encore importé → utiliser le nouveau bouton **autoroutage ∟** pour ré-orthogonaliser un schéma importé. ⏳ parsing du chemin Wokwi à faire.
12. ✅ Récupere les boards qu'on utilise sur wokwi et met les à l'echelle. → fait en v2026.6.30 : les éléments `@wokwi/elements` (Uno/Nano/Pro Mini/Mega) étaient référencés mais **Nano et Mega n'étaient jamais importés** (cartes vides) → importés dans `sim.mts` ; toutes les cartes AVR sont mises à l'échelle 10/9,6 (`pinScale`) pour un pas de broches de 10 px. Pico/Pico W : aucun élément Wokwi n'existe → dessin `parts/picow-module`.
13. ✅ les menu de la palette d'outils (ctrl + p) doivent apparaitre dans la langue de VScode → résolu en v2026.6.28 : localisation manifeste correcte (clés `package.nls` EN/FR empaquetées) ; si la palette reste en anglais, c'est que la langue d'affichage de VS Code est l'anglais (aucun bug, aucun changement de code).
14. ✅ Le dépot d'une carte à microcontroleur sélectionne l'outil de simulation. le menu deroulant de la barre d'outil n'est donc plus nécessaire. → fait en v2026.6.29 : déposer/poser un microcontrôleur (`onPartAdded`) bascule la carte de simulation sur celle déposée ; le menu déroulant de carte est **masqué** dans la barre d'outils.
15. ✅ Rajouter un bouton autoroutage qui retouche le dessin  pour n'avoir que des fils horizontaux et verticaux sue la selection. Si rien de sélectionné sur tout le dessin. → fait en v2026.6.29 : bouton **∟** (barre droite) → `autoRoute()` réécrit les fils en tracés H/V (un coude en L) ; sur les fils touchant la sélection si des composants sont sélectionnés, sinon sur tout le dessin.
16. ✅ Lors de déplacement de la sélection les coudes ne sont pas déplacés avec le reste de la sélection. Les coudes ne sont pas sélectionnable du tout. → fait en v2026.6.18 : les coudes des fils internes au lot suivent le déplacement du bloc (coudes sélectionnables depuis v2026.6.5, Suppr les retire).
17. ✅ Dans la fonction créer un composant, la fenetre apperçu doit être zoomable. Les coordonées de chaque pin editable directement → fait en v2026.6.29 : aperçu du créateur zoomable (boutons −/+, 25 %–600 %, les broches restent en coordonnées réelles) ; chaque broche a des champs X/Y éditables directement dans le tableau.
18. ✅ Le point de connexion avec les fils doit être le centre de la broche par exemple pour une broche qui ferait 2 de large le point de connexion doit être à 1 de 3 cotés. Si c'est un cercle oi un carré le centre. → vérifié en v2026.6.29 : la pastille est en `box-sizing: border-box`, centrée sur (pin.x, pin.y) ; `hotspotCenter` lit le centre de la pastille → le point de connexion EST le centre de la broche. Aucun cas décalé reproductible (corrigé depuis v2026.6.5).
19. ⏳ Si dans la selection on a une pico pi. Le déplacement vertical est conforme mais en horizontal elle la pico pi ne suit pas le mouvement → différé en v2026.6.29 : non reproductible par analyse statique (le déplacement applique le même vecteur X/Y à tous les membres du lot) ; à reprendre avec les étapes exactes (capture / console Outils de développement).
20. ✅ Aucune variable lisible ici ~~(en C : variables globales seulement)~~. Supprimer le texte barré pour la partie pico. Le mettre systématiquement en haut des variable (affichage permanent) pour le C. Un clic sur ce tecte ou une bulle d'aide explique comment créer des variables globales. → fait en v2026.6.29 : en C/Arduino, un **en-tête permanent** « ℹ Seules les variables globales sont affichées (cliquer pour l'aide) » coiffe le panneau (bulle d'aide + clic → page d'aide) ; en MicroPython cet en-tête est omis et le message « aucune variable » n'évoque plus le C.
21. ⏳ Ajouter les point d'arrêt conditionnels à la simulation arduino → hors périmètre (rappel v2026.6.17) : nécessite un évaluateur d'expressions DWARF côté AVR ; les points d'arrêt conditionnels restent disponibles en MicroPython, inconditionnels en C/AVR.
22. ⬜ Rajouter un onglet console de sortie la ou est le moniteur série avec un onglet pour afficher le résulats de la compilation. → reste à faire (non traité dans v2026.6.29).
# v2026.6.30
1. ✅ **Nano et Mega 2560 réparés** : les éléments `@wokwi/elements` correspondants n'étaient **jamais importés** dans `sim.mts` → la carte apparaissait vide. Importés (`arduino-nano-element`, `arduino-mega-element`). Le Pro Mini réutilise le visuel Nano.
2. ✅ **Pas des broches ramené à 10 px** : les cartes `@wokwi/elements` sont au pas physique 0,1″ ≈ 9,6 px, désaligné de la grille / platine (10 px). Nouvelle propriété `pinScale` (= 10/9,6) sur Uno/Nano/Pro Mini/Mega : le dessin **et** les pastilles sont agrandis pour tomber sur la grille de 10 px (export SVG inclus). `applyPinScale` agit une frame après le rendu Lit.
3. ✅ **Pico / Pico W = dessin `parts/picow-module`** : `@wokwi/elements` ne fournit aucun élément Pico (ni npm 1.9.2, ni dépôt GitHub). `<kablix-pico-board>` reprend désormais le SVG + 40 broches du composant `parts/picow-module.kablix-part.json` (deux rangées verticales au pas de 10 px, masses GND.1..8 renumérotées pour des pastilles distinctes). LED GP25 conservée en surimpression.
4. ℹ️ **`resolveJsonModule`** activé (tsconfig) pour importer le dessin Pico depuis `parts/` dans le bundle webview.
5. ⏳ **Placement de la LED GP25** sur le nouveau dessin Pico : approximatif (marge haut-gauche), à caler avec une vérification visuelle.
# v2026.6.29
1. ✅ **Afficheur 7 segments réparé** : il ne s'allumait jamais car le modèle cherchait un commun `DIG1` — or l'élément 1 chiffre de `@wokwi/elements` expose `COM.1`/`COM.2`. Le commun est désormais résolu (COM.1/COM.2/COM, repli DIG1). Nouvelle propriété **« Broche commune »** (cathode/anode) : en cathode commune un segment s'allume si sa broche est HAUTE et le commun BAS, en anode commune l'inverse.
2. ✅ **Cathode notée « K » sur toutes les diodes** : LED (C→K) et barre de LED (C1..C10 → K1..K10). Identifiants internes inchangés (simulation).
3. ✅ **Schémas internes barre de LED + 7 segments** : la barre affiche ses 10 diodes (A→K), le 7 segments une étoile de 8 diodes (A–G, DP) vers le commun, orientées selon cathode/anode. Visibles via le bouton ☢ d'un composant sélectionné.
4. ✅ **Potentiomètre : câblage inversé détecté** : si l'on permute VCC et GND sur les extrémités du rail, `potBindings` le repère (`inverted`) et la lecture analogique varie en sens inverse du curseur. (Les broches du potentiomètre ne sont plus colorées comme de l'alimentation depuis v2026.6.28.)
5. ✅ **Fils toujours par-dessus les composants** : le calque des fils passe en `z-index: 5` (au-dessus des composants et du câblage interne). Le SVG restant `pointer-events:none`, les broches dessous restent cliquables.
6. ✅ **Déplacement de l'extrémité d'un fil** : un fil sélectionné montre une poignée losange à chaque bout ; la glisser sur une autre broche (accrochage 14 px) reconnecte le fil.
7. ✅ **Bouton autoroutage ∟** : réécrit les fils en tracés strictement horizontaux/verticaux (coude en L), sur les fils touchant la sélection, ou sur tout le schéma si rien n'est sélectionné.
8. ✅ **Export SVG éditable** : chaque composant est exporté en `<g transform>` (groupe éditable dans Inkscape) au lieu d'un `<svg>` imbriqué (sous-document non éditable) ; le CSS du shadow DOM est réinjecté mais **scopé** à l'id du groupe (plus de fuite de styles d'un composant sur les autres).
9. ✅ **Sélection automatique de la carte au dépôt** : poser un microcontrôleur sur le canvas choisit la carte de simulation correspondante ; le menu déroulant de carte de la barre d'outils est masqué (devenu inutile).
10. ✅ **Créateur de composants : aperçu zoomable + coordonnées éditables** : zoom −/+ (25 %–600 %) de l'aperçu et champs X/Y directement modifiables pour chaque broche.
11. ✅ **Panneau Variables clarifié** : en C/Arduino, en-tête permanent « ℹ Seules les variables globales sont affichées » (bulle + clic → aide) ; en MicroPython, en-tête omis et message « aucune variable » sans la mention C.
12. ✅ **Recette de test** : nouveaux tests (fils par-dessus, déplacement d'extrémité, autoroutage, SVG éditable, sélection auto de carte, aperçu zoomable, cathode K, schémas internes, 7 segments corrigé, potentiomètre inversé, aide variables C) ajoutés en vert dans leur rubrique.
13. ℹ️ **Point de connexion = centre de la broche** : vérifié (pastille `box-sizing: border-box` centrée sur (pin.x, pin.y)). Aucun changement de code.
14. ⏳ **Différés** : largeur du curseur du potentiomètre à glissière (shadow DOM, à vérifier visuellement) ; import du routage orthogonal Wokwi (la rotation est déjà conservée) ; déplacement horizontal d'une Pico sélectionnée (non reproductible) ; points d'arrêt conditionnels en C/AVR (DWARF, hors périmètre) ; onglet console de compilation (à faire).
# v2026.6.28
1. ✅ **Buzzer : son réel (corrigé)** : le buzzer affichait la note animée mais restait muet — le son était piloté sur le **niveau instantané** de la broche, or un `tone()`/PWM oscille trop vite (l'échantillonnage par frame coupait le son en continu). Désormais le buzzer est considéré actif si la broche **bascule** (nouvelle détection `pulseActive` dans les moteurs AVR/RP2040, fenêtre 60 ms) — signal carré — ou, à défaut, s'il y a une tension continue. La fréquence reste mesurée sur la largeur d'impulsion. Son stable tant que `tone()` joue, coupé à `noTone()`.
2. ✅ **Pastilles d'alimentation colorées** : les broches VCC/5V/3V3… s'affichent en **rouge**, GND en **noir** (classes `pin--vcc`/`pin--gnd`), sur tous les composants — repère visuel de l'alimentation.
3. ✅ **Potentiomètre : broches 1 / 2 / V** : les extrémités du rail résistif ne sont plus étiquetées VCC/GND (trompeur) mais **1** et **2**, le curseur **V** (Variable) ; elles ne sont plus colorées comme des broches d'alimentation. Identifiants internes inchangés (simulation).
4. ✅ **Potentiomètre à glissière : sens corrigé** : son sens était inversé (curseur vers la masse → lecture forte). Inversion appliquée **uniquement au modèle à glissière** (le rotatif est conservé tel quel).
5. ✅ **Bulle d'aide du bouton** : pendant la simulation, l'info-bulle d'un bouton devient « **Ctrl+clic pour verrouiller l'état instable** » (hors simulation : déplacement au clic droit). Mise à jour dynamique au verrouillage.
6. ✅ **Avertissement de verrouillage déplacé** : le bandeau « ⚠ Simulation en cours : le câblage est verrouillé » est désormais affiché **en tête de la bibliothèque** (zone inutilisée pendant la simulation), largeur bornée par le panneau, pour l'AVR comme pour le RP2040.
7. ✅ **Export Wokwi : noms de cartes corrigés** : la carte Pico/Pico W est exportée sous le nom Wokwi **actuel** `board-pi-pico` / `board-pi-pico-w` (anciennement `wokwi-pi-pico…`) ; l'import accepte les deux (rétrocompatibilité). Tests `verify:wokwi` étendus.
8. ✅ **Recette de test : un seul format** : le `.csv` (redondant) n'est plus généré (ni versionné) ; seule la **recette `.docx`** est maintenue. Nouveaux tests (pastilles power, potentiomètre, noms Wokwi) ajoutés en vert dans leur rubrique.
9. ℹ️ **Palette de commandes (langue)** : la localisation manifeste est correcte (clés `package.nls` EN/FR sans BOM, empaquetées) → les commandes suivent la langue de VS Code. Si elles restent en anglais, c'est que la langue d'affichage de VS Code est l'anglais (à confirmer). Aucun changement de code.
# v2026.6.27
1. ✅ **Import/export Wokwi revu** : le **retournement** (flipH/flipV) et les **coudes des fils** étaient perdus à l'aller-retour `diagram.json`. Ils sont désormais conservés dans un bloc d'extension `kablix` (clé de premier niveau **ignorée par Wokwi**) : les `parts`/`connections` restent strictement au format Wokwi (donc ouvrables dans Wokwi), et un aller-retour **Kablix → diagram.json → Kablix** restitue flip et coudes à l'identique. `toWokwiDiagram`/`fromWokwiDiagram` réécrits (extension `KablixExtension`, coudes indexés sur la position de la connexion). Tests `verify:wokwi` étendus (flip + coudes aller-retour). Limite restante : composants personnalisés (`kablix-custom-part`) et types Wokwi inconnus toujours ignorés (comptés dans la barre d'état). Docs FR mises à jour.
# v2026.6.26
1. ✅ **Buzzer sonore** : un buzzer actif émet désormais réellement un son (Web Audio, oscillateur carré par buzzer). La **fréquence suit le signal** qui le pilote — `tone()`/PWM mesuré en largeur d'impulsion → f = 1e6 / (2 × largeur haute) ; à défaut d'un signal mesurable (broche maintenue haute) un bip par défaut (2 kHz) est joué. Contexte audio réveillé sur ▶ (geste utilisateur), tous les sons coupés à l'arrêt/réinitialisation. Nouveau `buzzerBindings` (borne → broche MCU). **Bug corrigé au passage** : sur AVR `readPulseUs` renvoyait toujours 0 (`lastUs` n'était jamais écrit dans `samplePulses`) → l'angle réel du servo et la fréquence du buzzer fonctionnent maintenant sur AVR comme sur RP2040.
2. ✅ **Breadboard à la grille de 10 px** : la rigole centrale (`CHANNEL`) et l'écart rails↔bloc (`RAIL_GAP`) passent de 14 à 20 px → tous les trous (et donc les broches) tombent sur la grille de 10 px, séparateurs compris.
3. ✅ **Export SVG : breadboard derrière les composants** : les platines d'essai sont dessinées en premier (donc en arrière-plan) ; une breadboard ajoutée après un composant ne passe plus devant lui.
4. ✅ **Export SVG : nom par défaut = nom du projet** : le dialogue de sauvegarde propose `<nom du projix>.svg` (dernier `.projix` ouvert/enregistré), sinon le nom du dossier de travail, sinon `schema-kablix.svg`.
5. ✅ **HC-SR04 : marquage des pattes retiré** : les noms VCC/Trig/Echo/GND sont déjà sérigraphiés sur le dessin → le générateur de composants ne superpose plus les étiquettes (option `padLabels: false`), les pastilles de connexion restent visibles. (Réimporter `parts/hc-sr04.kablix-part.json`.)
6. ✅ **Nouveaux composants au centre de la zone visible** : un clic sur un composant de la palette le pose au centre de la fenêtre visible (tient compte du zoom et du déplacement de la vue), corps centré puis aligné sur la grille — au lieu d'un coin en haut à gauche du monde.
7. ✅ **Recentrage hors des barres d'outils** : « recentrer & ajuster » réserve une marge supérieure (≈ 56 px) pour que le contenu ne se retrouve plus sous les barres flottantes du haut.
8. ✅ **Bulle d'aide du bouton** : l'info-bulle d'un bouton poussoir rappelle aussi le **Ctrl+clic** qui verrouille l'état enfoncé (transitoire au clic simple) en simulation.
9. ✅ **Avertissement de câblage verrouillé** : un bandeau persistant « ⚠ Simulation en cours : le câblage est verrouillé. » s'affiche en haut du canvas pendant toute la simulation (FR/EN).
10. ✅ **Aide : dossier de sketch Arduino** : note ajoutée (FR + EN) rappelant que, comme pour tout projet Arduino, le `.ino` de lancement doit être dans un dossier portant exactement le même nom (sinon `arduino-cli` refuse de compiler).
11. ✅ **Palette de commandes localisée** (vérifié) : tous les titres de commandes utilisent les clés `package.nls` présentes en EN et FR, empaquetées dans le `.vsix` → la palette (Ctrl+Maj+P) suit la langue de VS Code (= langue de Kablix). Aucun changement de code nécessaire.
# v2026.6.25
1. ✅ **Gestion du CS multi-SPI** : `selectSpiDevice` route chaque octet vers l'esclave dont la broche CS est active (bas) ; plusieurs périphériques SPI peuvent coexister sur le bus (sinon repli sur l'esclave sans CS). CS/D-C résolus par `spiDeviceBindings`. Validé (unitaire + bout en bout AVR : CS bas → ILI9341).
2. ✅ **Écran TFT ILI9341 (SPI couleur, 240×320)** : `Ili9341Device` décode CASET/RASET/RAMWR + pixels RGB565 (D/C) dans une image RGBA dessinée dans le canvas de l'élément `wokwi-ili9341`. Composant `ili9341` (kind `spi-tft`). Validé bout en bout (sketch SPI matériel + D/C + CS → pixel rouge) + unitaire.
3. ✅ **Carte microSD (SPI)** : `SdCardSpiDevice` — répondeur de PROTOCOLE (CMD0→idle, CMD8, CMD55/ACMD41→prêt, CMD58, CMD17/CMD24 lecture/écriture de blocs de 512 o vers 64 Ko de RAM). Composant `microsd` (kind `spi-sd`). Permet de détecter/initialiser la carte et de lire/écrire des blocs ; **aucun système de fichiers FAT préchargé** (ouvrir un fichier sur une carte vierge échoue, comme attendu). Validé par tests unitaires (init + aller-retour d'écriture/lecture de bloc).
4. ✅ **NeoPixel validé sur Pico réel** : correction du décodeur — le **dernier bit** d'une trame (sans front suivant) est désormais classé par un `flush()` (seuil ≈ 0,6×T1H de la trame), appelé à la lecture des couleurs. MicroPython `neopixel.NeoPixel(Pin(0), n).write()` sur le firmware Pico réel → couleurs décodées correctement (LED vert + rouge). NeoPixel est donc pleinement fonctionnel sur **AVR et RP2040**.
# v2026.6.24
1. ✅ **SPI simulé (AVR + RP2040)** : infrastructure d'esclave SPI (`setSpiDevices`) sur les deux moteurs — AVR via `AVRSPI.onByte`/`completeTransfer`, RP2040 via `mcu.spi[].onTransmit`/`completeTransmit`. Interface `SpiDevice` (transfert octet MOSI→MISO + niveau D/C). **OLED SSD1306 reconnu comme SPI** : l'élément `wokwi-ssd1306` expose en fait des broches SPI (DATA/CLK/DC/CS) → le composant `oled-ssd1306` passe en `kind: 'spi-oled'`, décodé par `Ssd1306Device.transfer(octet, dc)` (D/C lu sur la broche MCU via `spiOledBindings`) et rendu dans l'`imageData`. Validé de bout en bout sur AvrEngine : sketch SPI matériel + broche D/C → écran SSD1306 affiché. Un seul périphérique SPI géré (pas de gestion du CS pour l'instant). Nouveau `kind: 'i2c-oled'` conservé pour les OLED I²C 4 broches. Test unitaire du chemin SPI ajouté (`verify:i2c`).
# v2026.6.23
1. ✅ **NeoPixel (WS2812) simulé** : nouveau modèle `kind: 'neopixel'` (pixel, anneau, matrice). Décodeur `Ws2812Decoder` (`ws2812.mts`) alimenté par les fronts de la broche DIN (horodatés en cycles) : chaque bit est classé par **HAUT > BAS** (règle indépendante de l'implémentation, valable pour le bit-bang AVR comme pour le PIO), 24 bits = une LED (GRB), long BAS = reset de trame. Les couleurs décodées pilotent l'élément (`r/g/b`, `setPixel`). Liaison `neopixelBindings` (DIN → broche MCU, nombre de LED selon matrice/anneau/pixel). Validé de bout en bout sur AvrEngine (bit-bang WS2812 → LED0 vert, LED1 rouge) + test unitaire du décodeur. AVR pleinement fonctionnel ; RP2040 best-effort (dépend du toggle GPIO par le PIO).
# v2026.6.22
1. ✅ **Afficheur OLED SSD1306 (I²C) simulé** : nouveau modèle `kind: 'i2c-oled'`. `Ssd1306Device` décode le flux I²C (octet de contrôle 0x00 commandes / 0x40 données, adressage horizontal : plages colonnes 0x21 / pages 0x22, mode page 0xB0-B7) et remplit un tampon GDDRAM 128×64. L'UI recopie le tampon dans l'`imageData` de l'élément `wokwi-ssd1306` (blanc sur noir) → l'écran affiche réellement ce que le programme dessine. Gère les deux styles de pilote (Adafruit en flux, MicroPython une commande par transaction). Le composant `oled-ssd1306` passe en `i2c-oled` (adresse 0x3C). Validé par tests unitaires (`verify:i2c`). Fonctionne sur AVR et RP2040 (via l'I²C des deux moteurs).
# v2026.6.21
1. ✅ **I²C sur RP2040** : `PicoEngine.setI2cDevices` relie les esclaves I²C (LCD, PCA9685…) aux deux contrôleurs matériels `i2c0`/`i2c1` (mêmes décodeurs `i2c-devices.mts` que sur AVR). Les hooks `onStart/onConnect/onWriteByte/onReadByte/onStop` de `RPI2C` sont câblés vers le répartiteur par adresse. Validé de bout en bout : script MicroPython `machine.I2C(0).writeto(0x27, …)` → LCD « OK ». Les composants `i2c-lcd`/`i2c-pwm` fonctionnent donc maintenant sur Uno/Nano/Mega **et** Pico/Pico W.
# v2026.6.20
1. ✅ **LCD I²C simulé (protocole réel)** : le maître TWI du MCU (`AVRTWI`) est relié à des modèles d'esclaves I²C (`i2c-devices.mts`) routés par adresse. Le LCD (kind `i2c-lcd`) décode le backpack **PCF8574 + HD44780 4 bits** (verrouillage sur front descendant de E, paires de quartets, clear/home/set-DDRAM, écriture caractère) et le **texte est superposé sur le dessin** (`<kablix-custom-part>.setLcd`, zone écran réglable par attributs sx/sy/sw/sh). Validé de bout en bout : sketch bare-metal TWI → « OK » affiché.
2. ✅ **PCA9685 simulé** (kind `i2c-pwm`) : décode les registres I²C (auto-incrément) → rapport cyclique des 16 canaux ; `pca9685Bindings` repère les composants reliés à chaque canal et les **pilote** (servo : angle d'après l'impulsion 50 Hz ; LED : luminosité/on). Décodeur validé par tests unitaires.
3. ✅ Infra I²C : `setI2cDevices` ajouté à l'interface des moteurs (implémenté sur AVR ; RP2040 à venir). Nouveaux kinds `i2c-lcd`/`i2c-pwm` sélectionnables pour les composants perso. Composants `parts/` reclassés (lcd1602-i2c, pca9685). Tests : `npm run verify:i2c` (décodeurs LCD/PCA9685).
4. ⏳ **À suivre** : I²C sur RP2040 (Pico/Pico W) ; afficheurs OLED/NeoPixel ; SPI.
# v2026.6.19
1. ✅ **Servo : angle réel** d'après la largeur d'impulsion mesurée (1000 µs → 0°, 1500 µs → 90°, 2000 µs → 180°) au lieu de 0/90°. Mesure d'impulsion ajoutée aux moteurs (`readPulseUs`/`setPulseMonitors`, comptage de cycles : AVR `cpu.cycles`/16, RP2040 `core.cycles`/clkSys). Validé AVR (1500 µs → 90°) ; Pico best-effort. Repli 0/90° si pas de mesure.
2. ✅ **HC-SR04 simulé (protocole ultrason réel)** : nouveau modèle `kind: 'ultrasonic'`. À chaque impulsion TRIG (≥ 8 µs), le moteur génère sur ECHO une impulsion de largeur = distance × 58 µs **en temps simulé** (latence ~200 µs, plage 2–400 cm) ; distance réglable via l'attribut `distance` (cm, défaut 20). Liaison `ultrasonicBindings` (résout TRIG/ECHO → broches MCU). Le composant `parts/hc-sr04.kablix-part.json` passe en `ultrasonic` (pinRoles TRIG/ECHO). Validé de bout en bout sur AvrEngine (sketch TRIG + pulseIn → ~20 cm). Disponible sur la famille AVR (Uno/Nano/Mega) ; Pico à venir.
3. ⏳ **À suivre** (composants perso à protocole I²C — option « simulation protocole réelle ») : PCA9685 (trames I²C → 16 PWM → pilotage des composants reliés) et LCD I²C (I²C → texte affiché). Les cœurs exposent TWI/I²C (`AVRTWI`, i2c rp2040js) ; reste à écrire les modèles d'appareil I²C, la propagation des sorties (PCA9685) et un élément d'affichage texte pour le LCD.
# v2026.6.18
1. ✅ **Cartes Arduino Nano / Pro Mini / Mega 2560 + Raspberry Pi Pico W** ajoutées au sélecteur. Refonte autour d'une **famille de MCU** (`boardFamily` dans `catalog.mts`) : `avr328` (Uno/Nano/Pro Mini, moteur avr8js inchangé), `avr2560` (Mega), `rp2040` (Pico/Pico W). Le moteur AVR (`avr.mts`) gère désormais l'**ATmega2560** (ports A–L, SRAM 8 Ko pour la pile à RAMEND 0x21FF, broches 0–53 / A0–A15, ADC A0–A7) — USART0/timers 0-2/ADC sont aux mêmes adresses que le 328P. Compilateur : FQBN/`-mmcu` selon la carte (`arduino:avr:mega` + atmega2560, sinon uno/328p) ; Pico W → firmware MicroPython `RPI_PICO_W` auto. Validé : un blink ATmega2560 réel s'exécute dans avr8js (ports étendus OK). Limites Mega « partiel » : timers 3-5, USART1-3, ADC A8-A15 non simulés.
2. ✅ **Sélection au rectangle** : ne retient que les composants **entièrement** encadrés (contenance complète au lieu d'intersection) ; les **coudes des fils internes** au lot suivent désormais le déplacement du bloc (plus de tracé déformé).
3. ✅ **Variables rouges = delta du dernier arrêt** : à chaque relance (pas à pas ou ▶), les variables en rouge repassent en noir ; seules celles modifiées pendant ce pas repassent en rouge à l'arrêt suivant (fin du cumul jusqu'au redémarrage).
4. ✅ **Pont réseau réel Pico W** : `network` et `urequests`/`requests` MicroPython remplacés par un shim (`pynet.ts`) qui tunnelle les requêtes HTTP via la liaison série (`\x1bNT…` filtré par `pico.mts`) vers l'hôte VS Code (Node `fetch`), réponse réinjectée dans le script (`\x1bNR…`). WLAN factice « connectée » (IP 192.168.1.50). Réglage `kablix.picowNetworkBridge` (activé par défaut), HTTP(S) seul, corps plafonné 64 Ko, délai 15 s. Validé de bout en bout (`scripts/verify-net.mjs`).
5. ✅ **Aide : rubrique « Créer un composant avec une IA »** (FR + EN) : tuto + prompt prêt à copier qui produit un `.kablix-part.json` (rappel du pas de 10 px entre broches).
6. ✅ **Bibliothèque de composants partageable** : dossier `parts/` + `README`, générés depuis `media/parts/` par `scripts/build-parts.mjs` (`npm run build:parts`). Chaque composant = `.kablix-part.json` autonome importable via ⇪ Importer.
7. ✅ **Pas de 10 px** garanti : les nouvelles cartes réutilisent le pipeline existant ; le générateur de composants superpose des pastilles étiquetées strictement sur la grille de 10 px (validé : aucune broche hors grille).
8. ✅ **Composants depuis les SVG de `media/parts/`** : PCA9685, HC-SR04, LCD 16×2 I2C, Grove Shield (Pico), dessin Pico W → 5 `.kablix-part.json` (brochages issus des fiches techniques, pastilles superposées sur la grille).
# v2026.6.17
1. ✅ Fond du bouton gomme resté **blanc** au lieu de bleu → cause : la règle `.canvas-controls__btn--eraser` était déclarée **avant** la règle générique `.canvas-controls__btn` (même spécificité → la dernière déclarée gagnait, écrasant le fond bleu par le fond d'input). Correctif : règle déplacée **après** et passée en double classe `.canvas-controls__btn.canvas-controls__btn--eraser` (spécificité supérieure) → fond `rgb(150, 230, 240)` garanti.
2. ✅ Variables modifiées **en rouge pur (255,0,0)** : la valeur d'une variable qui change passe désormais en `rgb(255, 0, 0)` (au lieu de `var(--vscode-errorForeground)`, un rouge atténué dépendant du thème) ; le mécanisme (rouge jusqu'au prochain démarrage) était déjà en place depuis v2026.6.11.
3. ✅ **Points d'arrêt conditionnels** sans effet → l'hôte n'envoyait que les **numéros de ligne** (la condition `bp.condition` était jetée). Désormais : `sendBreakpoints` transmet `{ ligne, condition }` ; le protocole stdin `\x10…\n` passe en **JSON** `{ "ligne": condition|null }` (robuste aux virgules dans la condition) ; le préambule MicroPython évalue la condition avec `eval(cond, globals())` et ne suspend que si elle est vraie (erreur d'évaluation = pas d'arrêt, comme VS Code). En C/Arduino la condition n'est pas évaluée (arrêt inconditionnel — évaluateur d'expression DWARF hors périmètre). Type `Breakpoint` ajouté à l'interface des moteurs ; AVR/Pico adaptés. Validé de bout en bout par `node scripts/verify-debug-py.mjs` (arrêt respectant la condition + reprise au retrait).
4. ✅ **Langue de l'extension = langue de VS Code, repli anglais** : l'UI du simulateur était déjà localisée (clés sources anglaises, dico FR, repli anglais via `i18n.mts`/bundle l10n/package.nls). Manquaient (a) la **description par défaut** `package.nls.json` (était en français → passée en anglais ; la version FR reste dans `package.nls.fr.json`) et (b) la **page d'aide**, qui était **figée en français** : `src/help.ts` choisit désormais le corps selon `vscode.env.language` — français si commence par « fr », **anglais sinon** (titre, `lang`, sommaire et contenu intégral traduits, lien doc en ligne FR/EN). Mention des points d'arrêt conditionnels et de la coloration en rouge ajoutée aux deux versions.
# v2026.6.16
1. ✅ **Icône d'aide** dans la barre d'outils (en haut) : bouton « ❔ Aide » à droite de 🏷 Noms → ouvre la page d'aide (message `help` de la webview → commande `kablix.openHelp` côté hôte). Libellés FR ajoutés au bundle l10n.
2. ✅ **Bouton gomme à fond bleu** : le bouton effacer du canvas passe sur un fond `rgb(150, 230, 240)` (fond + bordure), l'image `Gomme.svg` occupant toujours 100 % du bouton.
3. ✅ Variable parasite `bdev : <Flash>` (et `vfs`…) dans le panneau Variables MicroPython → ce sont des noms **injectés par le démarrage de MicroPython** (block-device flash, systèmes de fichiers), pas des variables de l'élève : le préambule `__kx_vars()` les **masque** désormais (liste de noms `bdev/vfs/gc/os/sys/machine/rp2/st/fs` + types `Flash/Partition/VfsFat/VfsLfs1/VfsLfs2`).
4. ✅ Variable `compteur` (ou autre) qui n'apparaissait pas → cause probable : le balayage itérait `globals()` **en direct**, donc un objet « système » dont la lecture modifiait/perturbait le dict (`dictionary changed size during iteration`) interrompait la collecte et masquait les variables suivantes. Correctif : les clés sont **figées** (`list(globals())`) avant le balayage et **chaque variable est collectée dans son propre `try`** → un objet récalcitrant n'empêche plus jamais l'affichage des autres. Validé par `node scripts/verify-debug-py.mjs` (test de bout en bout : `compteur`/`seuil`/`nom` présentes, firmware réel).
# v2026.6.15
1. ✅ Panneau Variables MicroPython : un objet `Pin` (LED, bouton…) affichait son `repr` (« Pin(GPIO13, mode=OUT) ») → on affiche désormais **`nom.value`** = niveau logique 0/1 (le préambule `__kx_vars()` appelle `.value()` sur les objets qui en exposent une, hors types simples). Ex. : `led_verte.value = 1`.
2. ✅ Bouton « ⚙ Compiler » supprimé → **▶ Démarrer compile automatiquement** le fichier de code (cf. chip 📄) **si le source a changé** depuis la dernière exécution (comparaison du chemin + date de modification côté hôte), sinon relance le binaire déjà en mémoire ; les artefacts chargés directement (.hex/.uf2/.elf/.bin) sont relancés tels quels. Vaut pour Arduino/Uno **et** Pico. Message `runCached` ajouté entre l'hôte et la webview. Aide FR mise à jour.
3. ✅ Barre d'ajustement : finalement **aucune barre visible** (les zones se touchent, arrondis conservés) ; la zone de préhension reste élargie (±4 px invisibles) pour rester facile à attraper. (Annule le trait pointillé de v2026.6.14.)
# v2026.6.14
1. ✅ Points d'arrêt MicroPython sans effet (le pas à pas, lui, marchait) → cause : le moteur Pico n'implémentait pas `setBreakpoints`, donc l'appel optionnel du webview était ignoré ; le préambule `__kx` ne connaissait que le mode pas à pas. Désormais : nouveau protocole stdin `\x10 lignes \n` qui (re)définit la liste des arrêts ; `__kx(n)` se met en pause si `n` est un point d'arrêt, **même hors pas à pas** ; `pico.mts` implémente `setBreakpoints` (transmission immédiate si le script tourne, sinon dès l'entrée en exécution). Validé de bout en bout : arrêt sur breakpoint, ré-arrêt à chaque tour de boucle, retrait dynamique (le programme reprend et termine), pas à pas non régressé.
2. ✅ Aspect visuel de la barre d'ajustement → remplacée par un trait vertical **en pointillés** (« ⋮ ») de 2 px, centré sur toute la hauteur, dans une bande de 8 px facile à attraper (survol : couleur d'accent).
# v2026.6.13
1. ✅ Zones blanches de part et d'autre de la barre d'ajustement (« Composants — zone blanche — barre — zone blanche — canvas ») → le `gap: 1rem` de `.workshop` est supprimé : la barre d'ajustement (`.splitter`) est désormais directement accolée aux panneaux et au canvas, des deux côtés du canvas. (Préhension élargie conservée via `::before`.)
2. ✅ Avertissement « hors-ligne » dans l'aide + priorité de recherche du firmware → pour un fonctionnement 100% offline, placer le `.uf2` MicroPython **dans le dossier du projet** : Kablix cherche désormais **d'abord dans le workspace**, puis dans le firmware téléchargé/mémorisé (cache global), et ne propose le téléchargement qu'en dernier recours. Note ajoutée dans l'aide FR (`src/help.ts`) et dans les docs FR/EN.
3. ✅ (diagnostic + correctif) Script MicroPython bouton + LED qui ne faisait rien sur Pico → trois causes : (a) `from typing import Callable` plante MicroPython (`ImportError`, pas de module `typing`) — à retirer, `time.sleep_ms/ticks_ms/ticks_diff` sont natifs ; (b) `0 if led.value() else 1` ne bascule jamais car relire `.value()` d'une broche en sortie renvoie 0 — garder l'état en variable ; (c) **bug simulateur corrigé** : `readDigital` (pico.mts) testait `value === High`, donc une entrée `PULL_UP` au repos (état `InputPullUp`) était lue à 0 → boutons jamais « relâchés ». Lecture corrigée (sortie pilotée / signal d'entrée injecté). Validé de bout en bout (appuis → bascule LED).
# v2026.6.12
1. ✅ MicroPython sur Pico inutilisable faute de firmware (« Aucun firmware MicroPython (.uf2) trouvé… ») → l'extension est désormais **autonome** : au premier lancement d'un `.py`, si aucun firmware n'est trouvé, elle **propose de le télécharger automatiquement** (choix **Pico / Pico W**) depuis micropython.org (version officielle figée v1.28.0). Le firmware est mis en cache dans le stockage global de l'extension et **réutilisé dans tous les projets** — la question n'est posée qu'une fois. Replis : sélection d'un `.uf2` local, ou ouverture de la page de téléchargement si le réseau échoue. Nouveau module `src/firmware.ts` ; ordre de résolution (ajusté en v2026.6.13) : réglage `kablix.micropythonUf2` → scan du workspace → cache global → téléchargement assisté. Validé de bout en bout : le firmware v1.28 téléchargé boote dans le simulateur, exécute le script via raw REPL et fait clignoter GP25.
# v2026.6.11
1. ✅ Barre d'ajustement des zones trop large → réduite à un simple trait (1 px) ; zone de préhension élargie de façon invisible (`::before`) pour rester facile à attraper.
2. ✅ Variable changée en rouge jusqu'au **redémarrage** (et pas seulement jusqu'au pas suivant), y compris sur points d'arrêt → une variable qui change reste en rouge jusqu'au prochain démarrage de la simulation ; l'historique des changements est conservé entre les pauses (étapes ou points d'arrêt) et le tableau affiché est seulement masqué hors pause.
3. ✅ (partiel) La fenêtre de simulation s'ouvre à sa dernière taille → le simulateur rouvre dans **la dernière colonne d'éditeur utilisée** (mémorisée). La taille en pixels d'un éditeur webview n'est pas réglable par l'API d'extension (VS Code gère la disposition) ; rouvrir au même endroit restitue au mieux la taille.
4. ✅ Pattes de la carte Pico alignées sur la grille de 10 px → carte redessinée au pas de 10 px (= 0,1") au lieu de 16,6 px ; toutes les broches tombent désormais sur la grille.
5. ✅ Les fils disparaissaient à ~120 px du bord en élargissant la zone propriété → le SVG des fils passe en `overflow: visible` ; il ne rogne plus les fils dépassant sa largeur (le rognage réel reste fait par le canvas), comme pour les composants.
6. ✅ « Charger workspace » → « Charger binaire » (bulle « Charger un .uf2 (Pico) ou .hex (Arduino) déjà compilé ») ; réglage `kablix.showLoadBinaryButton` pour l'afficher, **masqué par défaut** ; documenté dans l'aide FR.
# v2026.6.10
1. ✅ Choix des couleurs des composants par boutons colorés → les propriétés « couleur » (LED, bouton, résistance, 7 segments, barre de LED…) s'éditent par pastilles colorées au lieu d'une liste déroulante (« GYR » de la barre de LED = dégradé vert/jaune/rouge).
2. ✅ Numérotation du todo qui recommence à 1 à chaque version → chaque section de version numérote ses items à partir de 1 (cette section l'applique).
3. ✅ Les composants alignent toutes leurs pattes sur la grille → au déplacement et au dépôt, on aligne la **première broche** sur la grille (10 px) ; les autres broches étant à des multiples du pas, elles tombent aussi sur la grille.
4. ✅ Nom du fichier de simulation enregistré dans le .projix → le manifeste `kablix.json` stocke `codeFile` (chemin relatif au workspace) ; à l'ouverture, le fichier est rétabli s'il existe, sinon son nom est affiché dans la pastille 📄.
5. ✅ Accents mal affichés dans le moniteur série → décodage UTF-8 incrémental de la liaison série (AVR + UART Pico) ; un caractère accentué émis octet par octet est restitué correctement, et la saisie vers le µC est encodée en UTF-8. (Sortie MicroPython via REPL : inchangée, protocole octet sensible.)
# v2026.6.9
- ✅ N'afficher que « nom : valeur » (sans le type), valeur en rouge si elle a changé → fait : panneau Variables sur 2 colonnes, la valeur passe en rouge quand elle diffère de la pause précédente.
- ✅ Le tableau de variables doit s'effacer quand on n'est pas en pas à pas → fait : le tableau est vidé dès que la simulation reprend / tourne (instantané périmé) et se remplit à chaque pause.
- « lastDebounceTime ne s'actualise pas en pas à pas » → comportement normal, pas un bug : le code ne fait `lastDebounceTime = millis()` que sur un front du bouton (`if (reading != lastButtonState)`) ; tant qu'on ne franchit pas cette ligne en pas à pas, la valeur ne change pas. La coloration en rouge (ci-dessus) rend ça visible.
# v2026.6.8
- ✅ Débogage erratique + aucune variable + LED qui ne s'allume pas (sketch anti-rebond) → diagnostic + correctifs :
  - **LED qui ne s'allume jamais = bug du code utilisateur** (pas le simulateur) : l'anti-rebond fusionne la lecture brute et l'état débouncé ; `lastButtonState` passe à LOW avant le test de front, donc `lastButtonState==HIGH && buttonState==LOW` n'est jamais vrai → la bascule ne se produit jamais (même sur un vrai Arduino). Correctif côté élève : séparer la lecture brute (`reading`) de l'état débouncé (`buttonState`).
  - « remonte de la dernière ligne au début de loop » = normal (loop() se répète).
  - Pas à pas erratique + variables manquantes : le `--optimize-for-debug` (-Og) restait trop optimisé. Nouvelle stratégie de compilation **en cascade** : 1) `-O0 -fno-lto` à la **compilation seule** (lien conservé en -flto, qui accepte les objets non-LTO) → pas à pas fidèle + globales lisibles ; 2) repli `--optimize-for-debug` ; 3) repli standard. La compilation n'est jamais cassée.
  - Diagnostic : le message « chargé dans le simulateur » indique désormais « débogage : N lignes, M variable(s) » (ou « avr-objdump introuvable ») pour voir si les infos de débogage sont bien extraites.
  - Rappel : les `const int` ne sont pas des variables en mémoire → elles ne s'affichent pas (normal) ; seules les globales non-const (ledState, lastButtonState…) apparaissent.
# v2026.6.7
- ✅ « il ne trouve plus arduino-cli » (régression v2026.6.6) → fait : le `-fno-lto` forcé via *extra_flags* cassait la compilation (édition de liens). Remplacé par le drapeau **officiel** `arduino-cli --optimize-for-debug` (cœur AVR en -Og : pas à pas plus fidèle + variables lisibles) **avec repli automatique sur la compilation standard** si ce mode échoue → la compilation ne peut plus être cassée par l'option de débogage.
# v2026.6.6
- ⬜ (à vérifier avec la toolchain) pour le débogage rien n'a changé : on n'atteint pas les premières lignes de loop, ça saute de la dernière à la première, suivi erratique → cause identifiée : le cœur AVR compile en `-Os` **ET `-flto`** ; la LTO ré-optimise à l'édition de liens et annulait le `-O0`. v2026.6.6 forçait `-O0 -fno-lto` (extra_flags) → **cassait la compilation** : corrigé en v2026.6.7 via `--optimize-for-debug` + repli.
- Explique « charger l'artefact du workspace » → réponse dans le chat (pas un changement de code) : charge dans le simulateur le binaire **déjà compilé** le plus récent du workspace (.hex pour l'Uno via `.vscode/arduino.json` ou scan ; .uf2 dans `build/` pour le Pico), **sans recompiler** — utile pour exécuter un binaire produit par une autre extension (Arduino, CMake…).
- ✅ bug d'affichage : le fil s'arrête aux bornes du rectangle de sélection au lieu de la patte, et on ne peut plus dessiner (apparu en câblant la 2e résistance) → fait : si un élément @wokwi publie son `pinInfo` après coup (rendu Lit asynchrone), les pastilles de broche manquantes sont (re)créées et repositionnées dans la passe différée (`syncHotspots`) → broches toujours cliquables, fils calés sur les vraies broches. (À revérifier ; si ça persiste, m'envoyer les étapes + erreurs de la console Outils de développement.)
- ✅ La grille de placement est trop grande → fait : pas ramené à 10 px (= écartement des broches / trous de platine `BB_STEP`) ; la grille de fond utilise le même pas.
- ✅ Le bouton central de la souris déplace la vue → fait : appui bouton du milieu = pan (déplacement de la vue), partout sur le canvas, même au-dessus d'un composant et pendant la simulation.
- ✅ Bouton « recentrer et ajuster » + 3 icônes dans une barre à droite alignée avec la barre de gauche → fait : barre droite (même hauteur/style que la barre de simulation) avec ⤢ recentrer-ajuster (zoom sur tout le schéma), ⟲ réinitialiser, gomme effacer.
- ✅ Icône de gomme trop petite → fait : l'image `media/Gomme.svg` occupe 100 % du bouton (`object-fit: contain`).
- ✅ Le volet Kablix ne doit pas s'ouvrir, le simulateur doit s'ouvrir directement → fait : clic sur l'icône Kablix → ouverture directe du panneau simulateur puis rebascule sur le volet Explorateur (le volet Kablix ne reste pas affiché). Garde-fou de démarrage conservé (pas d'ouverture au lancement).
- ⬜ (à vérifier) Aucune variable visible (globale ou locale) → même cause que le débogage : la LTO annulait le `-O0`, donc les variables restaient optimisées/éliminées. Le `-O0 -fno-lto` ci-dessus devrait rendre les **globales/statiques** lisibles. Les locales automatiques restent non gérées (analyse CFI DWARF). À retester sur ta machine.
# v2026.6.5
- ✅ Annule ça (verrouillage en pas à pas) : je veux actionner les composants (ex. BP) pendant la simulation et qu'ils restent dans la position où je les ai mis → fait : verrou de pause supprimé (plus de classe `canvas--paused`/pointer-events coupés) ; les composants restent actionnables même en pause/pas à pas. Maintien d'un BP : Ctrl+clic (mode « sticky » natif → la broche reste à LOW), clic simple = transitoire. Note d'aide ajoutée dans l'inspecteur du bouton.
- ✅ au debogage certaines lignes sont ignorées → fait : compilation **sans optimisation (`-O0`) par défaut** (arduino-cli via `--build-property compiler.optimization_flags=-O0` ; avr-gcc bare-metal en `-O0 -g3`). Sans `-Os`, le compilateur ne réordonne/n'élimine plus d'instructions → le pas à pas ne saute plus de lignes. (À revérifier avec la toolchain. Effet de bord : binaire plus gros — un très gros sketch pourrait dépasser les 32 Ko de l'Uno.)
- ✅ le ctrl+z modifie aussi le câble → fait : après un re-rendu (annuler/refaire, chargement) les fils sont recalés sur les broches définitives une frame plus tard (`scheduleSettle`) → plus de décalage. (À revérifier visuellement.)
- ✅ imprécision au bout des fils (le centre du bout du fil doit tomber sur le centre de la broche) → fait : pastilles de broche en `box-sizing: border-box` ; la bordure d'1 px ne décale plus le centre mesuré, le fil tombe pile sur (pin.x, pin.y).
- ✅ La fonction enregistrer le schéma modifie le câblage → fait : même cause/correctif que le Ctrl+Z (recalage différé des fils après re-rendu au rechargement). (À revérifier.)
- ✅ Le nom du composant doit apparaître au-dessus même en rotation → fait : le bandeau est repositionné après mise en page (`scheduleSettle`), donc placé au-dessus de la boîte englobante tournée même quand offsetWidth n'était pas encore connu au 1er rendu.
- ✅ le nom d'auteur (electropol.fr) dans la carte d'extension → fait : champ `author` passé en objet `{ name: "electropol.fr", url: "https://electropol.fr" }`. (L'éditeur affiché « franksauret » est l'identifiant de publication, fixé par le compte Marketplace — non modifiable localement.)
- ✅ Les variables n'apparaissent pas dans la zone variable en pas à pas → fait (en grande partie via `-O0`) : avec `-Os`, les variables étaient placées en registres / éliminées, donc invisibles ou fausses ; en `-O0` (ci-dessus) les variables **globales et statiques** vivent en SRAM à adresse fixe et sont lues correctement. Message « Aucune variable lisible ici » quand la liste est vide. Limite restante : les variables **locales automatiques** (déclarées dans `loop()`/`setup()`) ne sont pas affichées — il faut analyser la CFI DWARF (frame pointer) pour les localiser ; non implémenté car non vérifiable ici sans la toolchain (risque d'afficher des valeurs fausses). Contournement pédagogique : déclarer en global les variables à observer.
- ✅ Réinitialiser ne doit toucher qu'à l'état initial des composants, pas au câblage ; bouton tout à droite de la barre du canvas → fait : `resetVisuals` recrée les composants depuis leurs attributs initiaux (LED éteintes, interrupteurs/servos repositionnés) sans modifier les fils ; bouton ⟲ déplacé tout à droite (à gauche de la gomme).
- ✅ Lancer la simulation compile automatiquement si rien n'a encore été compilé → fait : ▶ déclenche une compilation du fichier de code si aucun programme n'a été chargé (la compilation enchaîne sur l'exécution) ; sinon démarrage direct. Réinitialisé au changement de carte.
- ✅ La couleur de fil par défaut (ni GND ni alim) ne doit être ni rouge ni noir → fait : rotation des couleurs sur `DEFAULT_WIRE_COLORS` (rouge et noir exclus) ; rouge réservé à VCC, noir à la masse — comportement automatique conservé.
- ✅ Pose des composants sur une grille magnétique pour faciliter l'alignement → fait : aimantation au pas de 20 px (aligné sur la grille de fond) au déplacement et au glisser-déposer ; un composant enfichable au-dessus d'une platine garde l'aimantation sur les trous.
- ✅ Remplacer le blanc par un fil fuchsia (255,0,220) → fait : couleur Dupont « blanc » remplacée par « fuchsia » `#ff00dc` (anciens fils blancs encore affichables).
- ✅ L'icône effacer du canvas n'est pas lisible → fait : remplacée par l'image `media/Gomme.svg`.
- ✅ Le volet Kablix:simulateur s'ouvre toujours au lancement → fait : suppression de l'ouverture automatique sur changement de visibilité du volet (qui se déclenchait aussi à la restauration de session). Plus aucune ouverture auto ; le sérialiseur ferme tout panneau restauré. Ouverture uniquement via un bouton d'accueil ou une commande.
- ✅ Changer le texte de la carte d'extension → fait : description par défaut (package.nls.json) passée au texte gaulois « Une application gauloise de simulation de microcontrôleurs (Arduino Uno / Raspberry Pi Pico) directement dans VS Code, 100% offline. ».
----
- ✅ Rends les zones de bibliotheque et de propriété/variable ajustable en largeur → fait : poignées de glissement entre la bibliothèque/inspecteur et le canvas (largeurs persistées).
- ---
- ✅ Un ensemble sélectionné doit pouvoir etre déplacé et copié dans le presse papier pour être dupliqué sur le schéma ou collé comme image vectorielle dans un autre logiciel → fait : Ctrl+C copie (presse-papier interne + SVG vers le presse-papier système), Ctrl+V colle (décalé), Ctrl+D duplique ; le SVG de la sélection est collable comme image vectorielle dans un autre logiciel.
- ✅ prévois un mode verouillage des actions temporaire pour la simulationen pas à pas - exemple appuis sur BP → fait : en pause/pas à pas, les composants ne réagissent plus (classe canvas--paused, pointer-events coupés).
- ✅ delay plante la simulation → fait : timers 0/1/2 ajoutés au moteur AVR (millis()/micros()/delay() fonctionnent, la boucle ne gèle plus).
- ✅ En simulation, mettre la zone variable à la place de la zone propriétés qui ne sert que pour le cablage → fait : pendant la simulation, le panneau Variables occupe la colonne de droite à la place des Propriétés.
- ✅ Ajoute la possibilité de sélectionner plusieurs composant avec une zone de la souris au ctrl + clic → fait : marquee (rectangle de souris) + Ctrl+clic ; déplacement, rotation, miroir et suppression de groupe.
- ---
- ✅ change l'auteur pour electropol.fr et rajoute les crédits de ce qu'on utilise tout en bas du readme. → fait : champ author = electropol.fr (package.json) + section « Crédits » en bas du README (avr8js, rp2040js, @wokwi/elements, JSZip, bootrom, MicroPython).
- ✅ Le volet Kablix:simulateur s'ouvre toujours au lancement et je veux qu'il soit masqué → fait : sérialiseur de webview qui ferme tout panneau restauré au lancement + garde-fou de démarrage (icône conservée, ouverture sur clic/commande).
- ✅ La fenetre principale de kablix doit s'ouvrir dans un nouvel onglet complètement à droite de la fenetre de vscode → fait : ouverture dans un nouveau groupe d'éditeurs (colonne = dernier groupe + 1).
- ✅ il y a des erreurs dans les fils lors de la sauvegarde → fait : les fils implicites d'enfichage (auto) ne sont plus tracés au rechargement (ils apparaissaient comme des fils parasites).
- ✅ Si la simulation est active, le rectangle de sélection et la barre de nom ne doivent pas apparaitre et on ne peut pas modifier le schéma. → fait : verrou de simulation (setLocked) — pas de sélection/déplacement/câblage/ajout/suppression ; les composants restent interactifs (BP).
- ✅ implémenter annulation et refaire sur ctrl z et ctrl y. → fait : pile d'historique (états sérialisés), Ctrl+Z / Ctrl+Y (+ Ctrl+Maj+Z).
- ✅ Modifier le format de fichier projix qui ne doit plus contenir que le schéma sans le code. → fait : .projix = manifeste + schéma + composants perso uniquement (plus de dossier code/).
- ✅ Le rectangle de sélection d'un composant doit tourner avec le composant → fait : contour porté par le corps (qui pivote).
- ✅ Rajouter dans la barre de simulation une icone pour tout réinitialiser. Les composants reviennent alors dans leur état initial comme s'il n'y avait pas de programme → fait : bouton ⟲ (arrête la simulation + recrée les composants à leur état initial).
- ✅ Change compiler & exécuter le fichier actif en Compiler → fait : bouton « ⚙ Compiler » + commande « Kablix : Compiler » (le détail passe en info-bulle).
- ✅ svg tjs hs. La carte arduino fait maintenant la bonne taille mais les texte dessus sont géants → fait : le CSS du shadow DOM (tailles de police) est réinjecté dans le SVG exporté.
- ✅ l'effet appuyé sur le bouton marche mais son état ne change pas → fait : les deux pastilles d'une même borne du bouton sont reliées dans la netlist (un fil sur 1.r/2.r compte comme sur 1.l/2.l).
- ✅ Supprime la croix d'effacement dans la barre de nom d'un composant → fait : ✕ retiré du bandeau (suppression via 🗑 inspecteur ou Suppr).
- ✅ Le cable doit être routé orthogonalement si le parcours l'est presque (10°) → fait : seuil d'aimantation H/V passé de 15° à 10°.
- ✅ pour les composants on doit pouvoir mettre des suffix m (milli), µ (micro) n (nano), p (pico), k (kilo), M (mega), G (Giga) dans la valeur → fait : champ valeur (résistance) accepte les suffixes SI (ex. 2.2k), affichage reformaté.
- ✅ Rajoute un bouton effacer dans le canva en haut à droite de la zone de canva visible → fait : bouton 🗑 en haut à droite du canvas (annulable avec Ctrl+Z).
---
- ✅ l'icone pour faire apparaitre le cablage interne n'est pas visible. Met la sur fond blanc et met le symbole radioactif jaune et noir → fait : pastille blanche avec le trèfle radioactif (jaune/noir) à gauche du ✕.
- ✅ si le composant est déselectionné la vue "interne" doit disparaitre → fait : le câblage interne n'est visible qu'à la sélection (l'état du bouton ☢ est mémorisé pour la prochaine sélection).
- ✅ L'icone catégorie dans la bibliothèque est illisible. Met une icone qui rappelle une arborescence ou une classification → fait : icône d'arborescence (nœud racine + 2 feuilles) à la place du 🗂.
- ✅ Quand on clic sur AZ pour le trie alphabétique il faut un séparateur entre les derniers utilisés et la liste alphabétique → fait : en-tête « Tous les composants » avant la liste alphabétique.
- ✅ l'export svg ne marche toujour pas la carte arduino est devenu minuscule maintenant → fait : taille lue sur le corps (div bloc) + repli sur la boîte écran ; plus de repli sur la valeur brute en mm (qui rendait la carte minuscule).
- ✅ laisse les traits de séparation dans le todo et si j'ai oublié rajoute en → noté : je conserve les `---` et en ajoute un après chaque lot traité.
- ✅ Il y a un problème, il ne trouves pas arduino-cli qui est pourtant installe → fait : résolution robuste (PATH + PATHEXT .exe, emplacements usuels dont l'Arduino IDE, réglages `kablix.arduinoCliPath` / `kablix.toolchainPath`). .ino → arduino-cli, .c/.cpp → avr-gcc.
---
- ✅ Il manque un bouton pour ouvrir un projix → fait : bouton « 📂 Ouvrir un projet (.projix) » ajouté à l'écran d'accueil (barre d'activité). Le bouton 📂 de la barre d'outils du simulateur existait déjà.
- ✅ Lors du glisser poser d'un composant, afficher le composant. → fait : la miniature du composant sert d'image de glissement (setDragImage), elle suit le curseur.
- ✅ Modifier la vue bibliothèque, une simple liste d'item avec une miniature du composant → fait : chaque item de palette = vignette (élément réel mis à l'échelle) + libellé.
- ✅ Remplace les fonctions miroirs et rotations de l'aide locale par des icones (en gardant le +- pour la rotation) → fait : barre « Orientation » de l'inspecteur, icônes ↺ ↻ (rotation 45°) et ⇆ ⇅ (miroir) ; touches +/− conservées.
- ✅ Modifie la fonction qui affiche le cablage interne pour que son activation ne se fasse plus lors de la sélection  mais par un bouton à gauche du x de suppression dans la zone avec le nom quand il apparait → fait : bouton 🔌 dans le bandeau (à gauche du ✕), bascule l'affichage ; plus de déclenchement à la sélection.
- ✅ Quand tu me mets les schéma interne utilise IEEE pas ANSI → fait : symbole de résistance interne passé du zigzag (ANSI) à la boîte rectangulaire (IEC/IEEE, usage français).
- ✅ si je déplace l'onglet (pour le mettre pleinécran  par exemple) il s'efface → fait : l'atelier (schéma, carte, noms) est persisté via vscode.setState/getState ; il survit au déplacement/rechargement de l'onglet. (Réouverture par commande = feuille vide, inchangé.)
- ✅ Ajout de la possibilité de supprimer des angles de fil avec la touche suppr → fait : cliquer un coude le sélectionne (croix accentuée), Suppr le supprime ; sinon Suppr efface le fil entier.
- ✅ Afficher dans le canva à coté de la barre de debug le nom du fichier qui contient le code et permet le debogage. Un clic dessus permet de le changer. → fait : chip 📄 dans la barre de simulation, affiche le fichier compilé/débogué, clic = choisir un autre fichier (boîte de dialogue).
- ✅ Certain composants ont une option "retournée" changer en "Retourner". Et étendre cette option à tous les composants. Proposer Sur l'axe horizontal et sur l'axe vertical. → fait : retournement H/V (miroir) pour tout composant via 2 boutons dans l'inspecteur (champs flipH/flipV du modèle, transform CSS).
- ✅ Dans la zone d'aide, pour les composants actifs, ajouter "Clic droit pour déplacer." → fait : ligne ajoutée à l'aide quand le composant est interactif.
- ✅ Déplacer les boutons de trie de la bibliothèque en dessous du mot Composants. → fait : titre « Composants » seul, boutons AZ/🗂 sur la ligne en dessous.
- ✅ half, mini et full pour la platine d'essai doivent être en français. → fait : affichés Mini / Moyenne / Grande (valeurs internes inchangées).
- ✅ Les composants sur la platine d'essai doivent bouger avec elle comme un groupe. De même que les composants enfiché les un dans les autres par exemple une pico pi sur une grove. → fait : déplacer un support entraîne tout ce qui est enfiché dedans (fils auto, transitif).
- ✅ une aide doit apparaitre pour l'edition de fils. → fait : aide affichée sous les propriétés quand un fil est sélectionné (croix/CTRL/double-clic).
- ✅ Rajouter une fonction de sauvegarde de fichier et faire un format de fichier zippé qui contient le dessin et le code extension ".projix" → fait : format ZIP .projix (kablix.json + diagram.json + dossier code/), commandes/boutons 💾 Enregistrer et 📂 Ouvrir, extraction du code optionnelle.
- ✅ Modifie le format des composants pour qu'il soit entièrement compatible avec celui de wokwi (si pas de problème de licence ou de coût). → fait : composants intégrés = éléments @wokwi/elements (déjà mêmes types/broches) + import/export du format projet Wokwi (diagram.json) via 2 commandes. Aucun souci de licence (format ouvert, éléments MIT). Limites : flip/coudes de fils et composants perso non convertis.
- ✅ Importe tous les composants wokwi → fait (liste choisie, 13 composants @wokwi/elements ajoutés) : afficheurs LCD 16×2 / 20×4, OLED SSD1306, NeoPixel / matrice / anneau (visuels) ; bouton poussoir 6 mm (interactif) ; capteurs analogiques NTC température, gaz MQ, pouls ; capteurs numériques flamme, son. Importables/exportables aussi en diagram.json. (Le reste du catalogue Wokwi — ili9341, mpu6050, dht22, hc-sr04, keypad, encodeur… — ajoutable à la demande.)
- ✅ Changer numerotage de version pour calver AAAA.MM.N° ex : 2026.6.10 → fait : version passée à 2026.6.0.
- ✅ on ne voit pas le bas de la page. Fais en sorte que la partie "envoyer un microcontroleur" soit le bas de la zone visible. → fait : mise en page pleine hauteur, le canvas absorbe l'espace et le moniteur série reste en bas.
- ✅ Les pattes de la led c'est A K pas A C. → fait : cathode affichée « K » sur les LED (identifiant interne 'C' conservé pour la simulation).
- ✅ Change le zoom pour un ctrl + molette → fait : zoom réservé à Ctrl + molette.
- ✅ Les boutons de simulation, démarrer, arréter, pause, pas à pas et vitesse doivent apparaitre sur le canva ; seulement des icônes, le texte dans une bulle → fait : barre flottante en haut-gauche du canvas, icônes seules + info-bulle.
- ✅ Pour un composant comme le bouton poussoir le cablage interne doit apparaitre au dessus du composant clairement visible lors d'un clic droit sur le composant. Pareil pour un ci ou les portes logique internes apparaitraient dessus au clic droit. → fait (selon précision : à la SÉLECTION, pas au clic droit) : câblage interne superposé (tracé noir sur fond blanc translucide) quand le composant est sélectionné/posé. Schémas : bouton poussoir, LED, résistance, buzzer (extensible aux CI/portes logiques quand ils existeront).
- ✅ On ne traduit plus l'aide pour l'instant on le fera à la fin. Fait seulement la version français et elle doit être en ligne et locale. Elle doit contenir une aide sommaire. → fait : commande « Kablix : Aide » (webview FR locale + lien en ligne GitHub).
- ⬜ (partiel) Changer l'image ascii art de l'interface par une copie d'écran avec du texte par dessus. → partiel : ASCII art remplacé par une image + légende. Vraie capture annotée à fournir (pas d'outil de capture/annotation côté agent) — il n'y a que des logos dans images/.
- ✅ compléter l'aide avec un pas à pas. Pour arduino (extension "Arduino-VsCode-IDE") et pico pi python (extension "Raspberry pi pico"). → fait : deux tutoriels pas à pas dans la page d'aide.


- ✅ Un glisser poser permet d'ajouter un composant directement la où on veut. → fait : glisser un bouton de la palette dépose le composant à l'endroit du lâcher (le clic seul le pose toujours au centre).
- ✅ En dessous de la fenêtre propriété ajouter une zone d'aide. La remplir par "+ ou - pour faire tourner le composant" quand un composant est sélectionné. → fait : encart d'aide en bas de l'inspecteur quand un composant est sélectionné.
- ✅ Permettre le zoom dezoom molette dans la zone de cablage. → fait : zoom molette centré sur le curseur (calque transformable), badge « ⟳ % » cliquable pour réinitialiser la vue.
- ✅ Corriger l'export svg qui est tout crado - Texte de l'arduino géant, fils coupés, composant manquant ou déplacé. → fait : chaque composant forcé à sa taille d'affichage (width/height + viewBox) donc broches alignées sur les fils ; cadrage englobant composants + fils + coudes avec marge ; repli rectangle étiqueté si pas de SVG.
- ✅ Proposer une mise à jour des différentes librairies (quand elles existent) en précisant que ça peut casser l'appli et que dans ce cas il faut fait une demande sur le github. → fait : commande « Kablix : Vérifier les mises à jour des bibliothèques » (+ réglage opt-in `kablix.checkUpdatesOnStartup`), interroge npm, avertit que la mise à jour peut casser et renvoie vers les issues GitHub.
- ✅ Ajuster la barre avec le nom au texte intérieur (le plus grands des 2). La déplacer en cas de rotation du composant → fait : bandeau large d'au moins le composant ou du nom (le plus grand), recalé au-dessus de la boîte englobante quand le composant est tourné.
- ✅ Le point blanc n'est pas visible il faut une couleur foncée ou plutot une croix (X) ombrée blanche avec ombre noire plus ou moins transparente. → fait : poignée de coude des fils transformée en croix X blanche avec halo noir, visible sur tout fond.
- ✅ La zone de nom doit disparaitre complètement lorsque les composants ne sont pas sélectionné. Elle peut apparaitre si le composant est sélectionné. Mais le vrai problème est que pour les composants actifs (comme le BP) on ne peut pas les déplacer sans cette zone parce qu'on ne peut pas les attraper. il faudrait que la zone non active permette de le attraper ou bien qu'on puisse les déplacer avexc un clic droit. → fait : bandeau visible à la sélection (ou bouton 🏷 pour tout afficher) + déplacement au clic droit pour tous les composants.
- ✅ Prévoir un mode débogage simple (pas à pas et lecture des variables) en c et python → fait : pause/reprise/ralenti, pas à pas, panneau Variables, points d'arrêt depuis la gouttière VS Code et surlignage de la ligne en pause. C via DWARF (avr-objdump), Python via instrumentation du source (préambule __kx + raw-paste). Détails dans docs/DEBOGAGE.md.
- ✅ Les fils doivent passer par dessus la carte arduino ou la carte pico pi.
- ✅ Le nom au dessus des composants doit pouvoir être masquable (bouton toggle dans le menu) mais la zone pour saisir et déplacer le composant reste.
- ✅ Feuille de dessin vide au démarrage.
- ✅ Ascenceur pour la bibliothèque des composants.
- ✅ Réorganise la bibliothèque. 2 façons possibles, ordre alphabétique, catégorie et en haut une zone avec les derniers utilisés (10 max)
- ✅ Rajouter une platine d'essai multi tailles elle doit mettre en surbrillance les broches connectées lors de la pose d'un nouveau composant.

