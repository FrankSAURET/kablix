# À faire
1.  Lorsque qu'on sélectionne une broche de la carte grove pico pi, la pico disparait il  faut qu'elle reste sur le dessus. Du rest c'est valable pout tous les composants ils passent en premier plan si on touche une de leurs broches et je en le veux pas.
1. un double clic dans l'explorateur sur un fichier projix doit l'ouvrir dans kablix.
1. Pour l'autoroutage, les equipotentielles son mal détectées. Tu vas donner un nom unique à chaque équipotentielle (eqp-x). chaque fil qui se connecte à une eqp-x sera renommé sous la forme eqp-x-y (x = numéro de l'équipotentielle et y numéro du fil) chaque fil aura donc aussi un nom unique. Si même eqp-x : possibilité de chevauchement et d'embranchement sinon pas possible. Supprime le point aux embranchements (n'en mets plus du tout). LEs fils ne doivent en aucun cas recouvrir une broche de connection à laquelle ils ne sont pas cablé.
1. sur l'alim, garde la fonction de coloration des fils mais supprime la pastille rouge et la noire sur les bornes. Le texte dans l'écran  doit être alligné à droite. Dans les propriétés, la valeur affichée doit varier avec seulement 2 chiffres aprés la virgule. 
# v2026.7.116
1. ✅ Module Grove « 16-Channel PWM Driver (PCA9685) » de Seeed en composant NATIF (item « remet-le dans Divers », dessin Fritzing retouché par Frank avec pastilles `connectorNNterminal`) : nouveau `<kablix-pca9685>` (catégorie Divers, 300×200 px), SVG nettoyé (namedview/attrs Inkscape retirés, ids TOUS préfixés `pca-`) dans `externe/pca9685.svg` ; le bloc servo P11/P12 décalé de +2,7 px dans le dessin d'origine est RECALÉ sur la grille au nettoyage (pire écart pastille↔broche mesuré : 0,1 px). Retiré du vieux générateur `build-parts.mjs` + `parts/pca9685.kablix-part.json` supprimé (collision de type).
1. ✅ 54 broches sur la grille de 10 px : connecteur Grove à gauche (GND=c50, VCC=c51, SDA=c52, SCL=c53 — conformes à l'item), bornier d'alim servo à droite (GND.2=c0 en haut, V+=c49 en bas), 16 colonnes servo P1..P8 (haut) / P9..P16 (bas) × 3 broches — signal `PWM0..PWM15` (P1 = canal 0, comme les bibliothèques), `Pn.5V` et `Pn.GND` colorées ROUGE/NOIR par `pinElectricalRole` (item « broches GND et power en noir et rouge »). Rails internes dans `buildNets` : masse commune Grove+bornier+colonnes, rail V+ bornier→colonnes 5V, VCC logique isolé — un servo enfiché sur une colonne est donc alimenté par l'alim du bornier (et compté 0,2 A par `psuLoadAmps`).
1. ✅ EXIGENCE D'ALIM EXTERNE (item « sans alim 5 V au courant suffisant, le composant ne doit pas fonctionner ») : nouveau `pca9685PowerState` (model.mts) — les sorties ne bougent que si le bornier V+ ET GND.2 est relié à une alim de laboratoire réglée entre 4,5 et 5,5 V (tension LIVE du bouton) dont le courant max couvre la charge (`psuLoadAmps`) ; sinon servos figés et LED/buzzers éteints (`applyPca9685` recalcule chaque frame), la puce continue de répondre sur I²C (VCC logique séparé — comme la vraie carte). Les PCA importés en `.json` (sans bornier) restent toujours alimentés.
1. ✅ Fichiers de test dans testkablix (item « fait ses fichiers de test ») : `pca9685-uno` (.ino, Wire.h pur — registres MODE1/prescale/LED0, pas de lib externe, compile en 4 028 octets) et `pca9685-pico` (.py, machine.I2C) — uno/pico + PCA + servo sur P1 + alim 5 V/1 A sur le bornier, le servo balaie 0°/90°/180° ; `_spec.mjs` (PART_PINS pca9685+alim) + `_verify.mjs` (nouveau cas `pca9685` : canal résolu + alim OK) — 852 contrôles verts.
1. ✅ Test de régression permanent `verify:pca` (ajouté à verify:all) : 23 contrôles — catalogue, rôles électriques, rails internes, binding canal 0, charge servo, 7 cas d'exigence d'alim (sans alim / 3 V / 12 V / live 8 V / courant insuffisant / GND.2 en l'air), 6 rendus réels Chrome headless (dessin, 300×200, 54 broches sur grille, positions clés, 54 pastilles de Frank appariées, pire écart 0,1 px). Capture visuelle validée (uno + PCA + servo P1 + alim 5,00 V câblés, couleurs auto). typecheck + build + verify:all OK (273 ✅), i18n FR.
# v2026.7.115
1. ✅ Alimentation de laboratoire (item « Créer une alim externe », dessin de Frank `media/parts/alim.svg`) : nouveau `<kablix-alim>` (catégorie Divers, kind `psu`), SVG nettoyé (namedview/path-effect/attrs Inkscape retirés, ids TOUS préfixés `alim-`) dans `externe/alim.svg`. Propriétés d'inspecteur « Tension (V) » (0-30, valeur de DÉMARRAGE) et « Courant max fourni (A) » ; prises banane V+ (rouge) / GND (noire) espacées de 20 px (pastilles du dessin) → dans la netlist l'alim est une SOURCE : V+ = rail haut, GND = masse (netLevel/resistiveGraph/netHasVcc — une LED s'allume sur l'alim SEULE, sans carte).
1. ✅ EN SIMULATION : le bouton du dessin se tourne à la souris (drag rotatif via `getScreenCTM` — zoom et rotation du composant compris, 0 → 30 V sur 300° horaire, dessin à 0 V soit 10°/V, zone morte de 60° collée à l'extrémité la plus proche, inerte en édition) ; rotation CSS sur un wrapper `alim-bouton-rot` injecté au nettoyage (le translate() Inkscape du groupe reste intact). L'écran affiche la tension courante `xx,yy` (police LED Board-7 des LCD, déjà embarquée) ; textes « Tension » / « Courant limite » du dessin TRADUISIBLES (remplacés par t('Voltage')/t('Current limit') au rendu). i18n FR.
1. ✅ LED « Courant limite » rouge vif + halo quand le courant débité dépasse le max : nouveau `psuLoadAmps` (model.mts, approximation pédagogique recalculée chaque frame) — pont résistif le plus direct V+→masse (fil direct = 99 A), chaque LED remontant au V+ de l'alim ((V−Vf)/R, 99 A en direct), 0,2 A par servo sur le rail V+, + `extraAmps` (prévu pour le PCA9685). Et la LED simple alimentée par l'alim suit désormais la VRAIE tension du bouton (`ledPowerCircuit` : minOhmsPath identifie la source atteinte — 30 V sur une LED + 220 Ω = grillée, 2,5 V sur une bleue = éteinte) ; la tension repart de la valeur de démarrage à chaque lancement.
1. ✅ Test de régression permanent `verify:psu` (ajouté à verify:all) : 26 contrôles — catalogue (Divers, props), netlist (LED sur alim seule, tension attr/live, source carte → null), 7 charges (14,5 mA, LED bleue non passante, pont 1 kΩ, court-circuit, LED directe, 2 servos, extraAmps), 13 rendus réels Chrome headless (dessin, 280×110, broches, écran 12,50, rotation 125°, libellés EN, bouton inerte en édition, drag 320° → 20 V + input + écran 20,00, LED rouge+halo/restaurée, reset tension). Captures visuelles validées (4 alims : 0/15/30 V + surcourant ; zoom bouton 0/7,5/15/30 V). typecheck + build + verify:all OK (251 ✅).
# v2026.7.114
1. ✅ Grove Shield pour Pi Pico intégré en composant NATIF (item « Grove pour Pico Pi », dessin de Frank `media/parts/Grove pour Pico Pi.svg`) : nouveau `<kablix-grove-pico>` (catégorie Cartes & platines), SVG nettoyé (namedview/path-effect/attrs Inkscape retirés, ids TOUS préfixés `gvp-` + classes `gvp-cls-` — l'export SVG fusionne les shadow DOM, même piège Fritzing que la Pico W) dans `externe/grove-pico.svg`. 126 trous `connectorNNterminal` de Frank PILE sur la grille de 10 px → `pinInfo` partagé avec la netlist (`diagram/grove-shield.mts`) ; connecteur DEBUG (hors grille, sans terminal) volontairement non câblable. Retiré du vieux générateur `build-parts.mjs` (le `.kablix-part.json` de même type entrait en collision).
1. ✅ ENFICHAGE de la Pico / Pico W sur le socle central (item « il permet de connecter directement la pico pi (et w) dessus ») : les cartes pico/picow deviennent enfichables (elles seules parmi les MCU) et UNIQUEMENT sur les 40 trous du socle (ports Grove et SPI = prises femelles, exclus) — mêmes fils `auto` que la platine d'essai, recalage pile sur les trous, GP25 (LED interne) jamais enfichée, surbrillance du trou visé pendant la pose (`setHighlight`), déplacer le shield emmène la Pico (grappe), shield dessiné SOUS la Pico (`part--shield`, z=0, + tri de l'export SVG).
1. ✅ E/S REDIRIGÉES conformément au schéma officiel Seeed v1.0 (`groveShieldStrips`) : I2C0=GP8/GP9, I2C1=GP6/GP7, UART0=GP0/GP1, UART1=GP4/GP5, A0/A1/A2=GP26/27/28 (2e signal du port = analogique précédent, A0.NC isolé), D16/D18/D20=GP16..GP21, SPI0 2×3 = SCK GP2 / TX GP3 / RX GP4 / CS GP5 (partagées UART1), rangées de dégagement `.b` = même signal, rail GND unique, rail 3V3 fixe pour les ports analogiques + SPI ; pastilles VCC/GND colorées via `pinElectricalRole` étendu aux noms suffixés (`I2C0.GND`, `A0.3V3`…).
1. ✅ Interrupteur 5V/VCC/3V3 ACTIF et MOBILE (item « switch actif et mobile ») : le curseur du dessin (`gvp-curseur-switch`, nommé par Frank) glisse entre 5V (gauche, dessiné) et 3V3 (droite, défaut — les GPIO de la Pico ne tolèrent pas 5 V) avec transition douce ; clic sur la zone du switch = bascule (pointerdown stoppé : pas de déplacement du shield), attr `pwr` PERSISTÉ dans le schéma (event `pwr-change` → attrs + inspecteur resynchronisé) et aussi réglable dans l'inspecteur (« Alimentation des ports Grove (VCC) ») ; la netlist suit : VCC des ports I2C/UART/D16-D20 relié au rail 3V3 ou au 5V (VBUS de la Pico). i18n FR.
1. ✅ Test de régression permanent `verify:grove` (ajouté à verify:all) : 37 contrôles en Chrome headless sur le vrai éditeur — rendu du dessin, 126 trous sur la grille + positions sondées, switch cliqué deux fois (attr, persistance, curseur), 10 liaisons internes du schéma Seeed, rails GND/3V3/VCC commuté, enfichage réel de la Pico (40 fils auto, recalage 0 px, VBUS→5V, GP16→GP16, grappe de déplacement), redirection de bout en bout (LED sur port D16 → net GP16 de la Pico ; I2C0.VCC → net 3V3 de la Pico). Captures visuelles validées (2 shields, Pico W enfichée, LED câblée sur D16, zoom du switch dans les 2 positions). typecheck + build + verify:all OK (225 ✅).
# v2026.7.113
1. ✅ Autoroutage — fils d'une même équipotentielle SUPERPOSÉS (item « fils de même équipotentiel se recouvrent ») : la netlist (`buildNets`) classe chaque fil par net ; dans l'A\* et le coût de départage, les fils des AUTRES nets restent interdits de chevauchement (créneau anti-superposition inchangé) mais suivre un fil du MÊME net devient un BONUS (remise `RIDE` = 75 % de la longueur couchée sur la dorsale, heuristique réduite d'autant pour rester admissible, lignes de Hanan des dorsales injectées pour se poser PILE dessus). Le fil « monte » donc sur le tracé existant et s'en détache au plus près de sa broche → embranchements, moins de coudes ; deux fils entre les mêmes broches se superposent en ligne droite. Fils `auto` (invisibles) jamais dorsale.
1. ✅ Embranchements marqués d'un POINT de la couleur du fil (item « marqués par un point ») : nouvelle passe `updateJunctions` (recalcul en microtâche après chaque positionWire/suppression/recoloration) — un coude d'où partent ≥ 3 directions distinctes en comptant tous les fils du net (jonction en T : entrée/sortie de dorsale, T posé à la main) reçoit un cercle SVG (r 3,4 px, `fill` = couleur Dupont du fil) dans le groupe `.wire-junctions` au-dessus des fils ; broches exclues (la pastille marque déjà la connexion), deux fils qui tournent ensemble (2 directions) exclus.
1. ✅ Correctif A\* au passage : les corps « tolérés » (bloc gonflé contenant une borne, exclus de `solid` pour laisser la broche sortir) sont désormais TAXÉS ×20 par px traversé dans le corps nu — sans cela, le chevauchement même-net permis faisait filer un fil TOUT DROIT à travers une LED posée sur la ligne (le repli créneau ne jouait plus).
1. ✅ `verify:route` étendu à 12 contrôles : dorsale droite + branche même net qui recouvre ≥ 50 px + point d'embranchement sur la dorsale + doublon mêmes broches superposé droit + T manuel → point pile à (660,470) à la couleur du fil + corps sur la ligne contourné sans traversée. typecheck + build + verify:all OK.
# v2026.7.112
1. ✅ Éditeur de composant — liste « Catégorie » (item « assigner le nouveau composant ») : nouvelle liste déroulante sous le nom (catégories standard de la palette : Cartes & platines, Discrets, Afficheurs & LED, Commandes, Capteurs, Actionneurs, Divers — libellés i18n), défaut « Composants personnalisés ». Un composant catégorisé est RANGÉ dans sa catégorie de la palette (en gardant sa ligne à boutons ✎/⇩/✕) ; sans catégorie, il reste dans la section « Composants personnalisés ». `category` persistée avec le composant, embarquée dans l'export/import `.json` (validée à l'import : clé inconnue ignorée).
1. ✅ Éditeur de composant — bouton « Soumettre à Kablix… » (item « bouton soumettre ») : à gauche des actions, ouvre une fenêtre expliquant comment envoyer le composant à Frank — exporter le `.json` (bouton ⇩ de la palette) puis issue GitHub avec le modèle « Submit new component » (bouton « Ouvrir le formulaire GitHub » → lien direct `issues/new?template=submit-new-component.md`, celui créé par Frank ce matin) ou pull request. La liste blanche `openExternal` du panneau s'étend au seul dépôt `github.com/FrankSAURET/kablix` (elle n'acceptait que docs.wokwi.com — audit v92 respecté). i18n FR.
1. ✅ `verify:selection` étendu à 23 contrôles : composant personnalisé AVEC catégorie rangé dans sa section standard (Capteurs), SANS catégorie dans « Composants personnalisés » (vrai éditeur + vraie palette). Capture visuelle validée (liste Catégorie + fenêtre de soumission). typecheck + build + verify:all OK.
# v2026.7.111
1. ✅ Message « Projet sauvegardé » (item « message de validation ») : à chaque enregistrement réussi du `.projix` (bouton Enregistrer, Enregistrer sous, commande, Ctrl+S), l'hôte notifie l'atelier (`projectSaved`) qui affiche « Projet sauvegardé » dans le statut de la barre d'outils pendant 3 s puis restaure le statut précédent (sauf s'il a changé entre-temps — lancement de simulation…). S'ajoute au message de la barre d'état VS Code.
1. ✅ Ctrl+S / Cmd+S dans l'atelier = Enregistrer (item « Ctrl+S sauvegarde ») : même chemin que le bouton — écriture directe dans le `.projix` courant, boîte de dialogue seulement sans projet connu ; `preventDefault` pour court-circuiter la sauvegarde d'éditeur de VS Code. typecheck + build + verify:all OK.
# v2026.7.110
1. ✅ Claviers « touches dures » intégrés (item « claviers alternatifs membrane/touche », dessins de Frank `keypad-{3,4}col-touche.edit.svg`) : case à cocher « Touches dures (au lieu de membrane) » dans l'inspecteur (nouveau kind `checkbox` des propriétés — attr posé à `1`/vidé, case dans le libellé) → le clavier bascule entre les 4 dessins (membrane/touche × 3/4 colonnes). SVG nettoyés (namedview À CONTENU, éléments inkscape:path-effect, attributs sodipodi/inkscape) et ids TOUS préfixés `kpt3-`/`kpt4-` + références url()/href réécrites (les deux fichiers Inkscape partageaient les mêmes ids — même piège que NTC/PTC v102, et l'export SVG fusionne tout). Pastilles `kpt*-pin-R1..C4` de Frank PILE sur la grille aux MÊMES positions que la membrane (80..140@320 / 110..180@320) → `pinInfo`, netlist et moteur INCHANGÉS (contrat button-press/release conservé). Touches recâblées par BOÎTE RENDUE (2 rects superposés de 32,87 px par touche, dédoublonnés par centre — robuste aux 4 touches hors groupe de la colonne D du dessin 4col) ; appui = capuchon assombri + enfoncé 1 px (brightness, le filtre url(#shadow) générique neutralisé car préfixé). Schémas internes de Frank (`interne/keypad-{3,4}col-touche.schema.svg`, même viewBox) branchés dans internal-wiring selon la case. i18n FR ; `.edit.svg` archivés dans `svg retouche/Validé/`.
1. ✅ Test de régression permanent `verify:keypad` (ajouté à verify:all) : 15 contrôles en Chrome headless sur le vrai élément — membrane 4col inchangée (16 touches, appui 5 → {5,1,1}), touche 3col (dessin kpt3 rendu, 12 touches, ordre 1..#, appui 8 → {8,2,1}), touche 4col (16 touches, colonne A-D, appui D → {D,3,3}), broches identiques membrane/touche ET sur la grille, pastilles dessinées pile sous les broches, bascule dynamique membrane→touche, schéma interne dédié. Captures visuelles validées (3 claviers + schéma superposé calé).
1. ✅ Icônes retouchées par Frank committées (item « enregistrerSous + serialMonitor modifiées ») : vérifiées en rendu 48/20 px sur fonds sombre et clair.

# v2026.7.109
1. ✅ Curseurs de simulation affichés SUR les composants (item « les curseurs doivent plutôt s'afficher sur les composants ») : le contrôle `simControl` (curseur/valeur, et les 2 lignes du DHT22) n'est plus posé SOUS le dessin mais PAR-DESSUS, centré verticalement (`top: 50%` + `translateY(-50%)`, toujours HORS FLUX — le correctif anti-décalage de rotation v2026.7.98 est conservé) avec un fond blanc translucide arrondi pour rester lisible sur n'importe quel dessin. Correctif central `sim-control-styles.mts` (couvre capteurs intégrés ET composants personnalisés) + styles locaux du DHT22 ; le `top:100%` du tilt (flèche d'infobulle) non concerné.
1. ✅ `verify:simshift` étendu : chaque cas exige désormais EN PLUS le chevauchement contrôle/dessin (le contrôle est bien sur le composant, pas dessous) — 7 cas, delta toujours (0,0) px. Capture visuelle validée (LDR/CTN/DHT22 : curseurs centrés lisibles). typecheck + build + verify:all OK.
# v2026.7.108
1. ✅ Bouton « Noms » à deux cases à cocher (item « bouton Noms ») : le bouton de la barre d'outils ouvre désormais un petit menu — « Tous les noms » / « Uniquement les composants sélectionnés », cases EXCLUSIVES mais toutes deux décochables : aucune cochée = AUCUN nom affiché, même sur un composant sélectionné (nouveau mode). Trois modes `all`/`selected`/`none` portés par les classes `canvas--show-labels` / `canvas--labels-sel` (l'ancienne règle CSS affichait inconditionnellement le bandeau du composant sélectionné) ; le poster de brochage garde la priorité (bandeau masqué). Défaut = « sélection seule » (comportement historique) ; préférence persistée (`labelsMode`) avec migration de l'ancien booléen `showLabels` (état webview ET uiState hôte) ; bouton surligné dès qu'un mode affiche des noms ; menu fermé au clic ailleurs ; i18n FR.
1. ✅ `verify:selection` étendu à 21 contrôles : les 4 modes vérifiés sur le vrai éditeur + la vraie feuille CSS — aucune case → aucun nom même sélectionné ; « sélection seule » → nom du seul sélectionné ; « tous » → tous ; poster de brochage → bandeau masqué malgré tout. typecheck + build + verify:all OK.
# v2026.7.107
1. ✅ Tmin/Tmax du curseur de température des CTN et CTP (item « paramètres Tmin et Tmax ») : deux nouveaux paramètres d'inspecteur par instance (« Tmin du curseur (°C) » / « Tmax du curseur (°C) », défaut −55/+125) ; le curseur de simulation suit ces bornes (attributs `tmin`/`tmax` relayés aux forks `kablix-ntc`/`kablix-ptc`, plage du `input range` dynamique). Bornes assainies dans le composant : attribut absent/invalide → défaut, min ≥ max → min ramené sous max (curseur toujours utilisable) ; les schémas déjà enregistrés sans ces attributs gardent la plage d'origine. i18n FR.
1. ✅ `verify:divider` étendu à 34 contrôles : plage par défaut −55..125 conservée + Tmin/Tmax d'instance suivis (CTN 0..50, CTP −10..90 via setAttribute) + bornes incohérentes (80/50) assainies. typecheck + build + verify:all OK.
# v2026.7.106
1. ✅ Autoroutage : la LIGNE DROITE est prioritaire (item « préférer une ligne droite plutôt que plusieurs coudes ») : deux broches alignées H ou V dont le segment direct est dégagé → AUCUN coude, même au ras des composants — l'ancien routage sortait toujours en « stub » perpendiculaire (ex. pattes sous le corps d'une CTN/CTP : Π à 2 coudes sous les composants) ou laissait des coudes colinéaires inutiles sur la ligne. Garde-fous : les corps des DEUX composants d'extrémité tolèrent ~1 pas de grille de chevauchement chacun (la broche vit au bord de son corps) mais un fil droit qui TRANCHERAIT un corps de part en part (ex. deux LED superposées, broches sous le corps) dépasse ce plafond et repasse par le routeur A* ; un composant tiers sur la ligne ou un fil déjà couché dessus → routeur normal (contournement / créneau anti-superposition).
1. ✅ Test de régression permanent `verify:route` (ajouté à verify:all) : 6 contrôles en Chrome headless sur le vrai éditeur — droit H (CTN→CTP), droit au ras des corps d'extrémité (CTP→LDR), composant tiers sur la ligne → contournement, ligne occupée par un fil → créneau, droit V (CTN tournées 90°), V qui percerait le corps d'arrivée → détour. Contre-épreuve faite sur le code d'avant : 2 échecs détectés (coudes inutiles sur ligne droite). Au passage le banc a révélé que `autoRoute` ne route que la sélection quand elle existe (comportement voulu, désélection dans le banc). typecheck + build + verify:all OK.
1. ✅ Icônes retouchées par Frank intégrées (item « serialMonitor.svg / serialTracer.svg ») : déjà branchées dans l'interface (bouton + en-tête du moniteur série, bouton + en-tête du traceur) — vérifiées en rendu Chrome headless aux 3 tailles d'usage (64/20/14 px) sur fonds sombre ET clair (invite `>_` bleue, courbe orange à halo jaune lisibles partout), puis committées telles quelles.
# v2026.7.105
1. ✅ Broches recollées sur la grille de 10 px (item « LDR, PTC et NTC mal alignées ») : cause racine trouvée dans le projet d'essai rv.projix — la ROTATION (et le miroir) tournait autour du centre de la BOÎTE MESURÉE du composant (1 px de gap de mise en page sous le dessin, dimensions parfois impaires après pinScale) SANS re-snap : les broches quittaient la grille (~2 px mesurés sur LDR/CTN/CTP/LED à 90°, 4,4 px sur une résistance) et les `.projix` enregistraient des positions fractionnaires (LDR à x=2198,5). Correctif : `rotateSelection` et `flipSelection` recollent le premier pin de chaque composant transformé sur la grille (snap silencieux, pas d'entrée d'historique parasite) ; au CHARGEMENT d'un schéma, réalignement doux de tous les composants (passes idempotentes au fil du settle + balayages différés 120/350/800 ms — la taille d'un dessin Lit peut encore bouger après les frames du settle, ex. police chargée tard, et les rAF seuls rataient un recollage à 0,5 px près), ce qui répare aussi les `.projix` déjà enregistrés avec l'ancien défaut.
1. ✅ Test de régression permanent `verify:align` (ajouté à verify:all) : 27 contrôles en Chrome headless sur le vrai éditeur — pose palette, rotations cumulées 90/180/270 et 45° (premier pin), miroir H, chargement d'un schéma « sale » aux positions réelles de rv.projix (LDR tournée x=2198,5 → broches recollées). Contre-épreuve faite sur le code d'avant : 21 échecs détectés. typecheck + build + verify:all OK.
# v2026.7.104
1. ✅ Bouton « Enregistrer » + « Enregistrer sous » (item « bouton enregistrer ») : nouveau bouton Enregistrer (disquette) qui écrit DIRECTEMENT dans le `.projix` courant sans poser de question (chemin mémorisé `projectUri` — posé à l'ouverture d'un projet et à chaque enregistrement, oublié par « Nouveau projet ») avec message TEMPORAIRE dans la barre d'état (4 s, pas de toast) ; sans projet connu, il ouvre la boîte d'enregistrement comme avant. L'ancien bouton devient « Enregistrer sous… » (icône disquette + crayon `media/enregistrerSous.svg`, dérivée du dessin de Frank, même palette) : boîte de dialogue systématique, préremplie sur le `.projix` courant. La commande `kablix.saveProject` (palette) suit le nouveau comportement Enregistrer. i18n FR (« Enregistrer le projet sous… »).
1. ✅ Vérifié : typecheck + build + verify:all OK ; icônes contrôlées en rendu (disquette seule vs disquette+crayon lisibles à 20 px).
# v2026.7.103
1. ✅ Visualisation renforcée de la sélection (item « composants, câbles et coudes sélectionnés ») : fil sélectionné = « fourmis en marche » (deux tracés pointillés superposés sombre/clair en alternance, décalés d'une demi-période, resynchronisés à chaque déplacement par positionWire) + halo d'accent (drop-shadow --vscode-focusBorder — l'ancien halo blanc était invisible sur fond clair) ; coude sélectionné = croix blanche sur disque d'accent cerclé (l'ancienne croix teintée se distinguait mal des autres coudes) ; composant sélectionné = pointillé existant + halo d'accent (box-shadow, repérable même sur composant sombre). S'applique aussi aux lots (Ctrl+clic fils, Ctrl+clic/marquee composants, marquee coudes) ; neutralisé en simulation (canvas--locked) et nettoyé à la suppression/désélection/chargement (helpers centraux setWireHighlight/dropWirePath dans editor.mts).
1. ✅ Test de régression permanent `verify:selection` (ajouté à verify:all) : 17 contrôles en Chrome headless sur le VRAI éditeur + la VRAIE feuille styles.css — classe + fourmis (2 tracés au même `d` que le fil), styles calculés (dasharray/animation/2 teintes/drop-shadow/disque du coude/outline+halo du composant), resynchronisation des fourmis à l'insertion d'un coude, exclusivité fil/composant, lots Ctrl+clic posés/retirés, désélection au clic sur le fond, Suppr = fil ET fourmis hors du DOM. Contre-épreuve faite sur le code d'avant : 7 échecs détectés. Capture visuelle dark/light validée. typecheck + build + verify:all OK.
# v2026.7.102
1. ✅ LDR, CTN et CTP nues intégrées (item « LDR + CTN + CTP avec paramètres », dessins de Frank `svg retouche/{LDR,NTC,PTC}[-schema].edit.svg`) : forks maison `kablix-ldr` (100×60, pattes 1/2 à (10,30)/(90,30)) / `kablix-ntc` / `kablix-ptc` (40×80, pattes à (10,70)/(30,70)), SVG nettoyés (script polyfill mesh retiré, ids préfixés `ldr-`/`ntc-`/`ptc-` — NTC et PTC partageaient les MÊMES ids Inkscape, collision de gradients sinon), pastilles sur la grille de 10 px. Paramètres dans l'inspecteur : LDR = résistance à 1 lx + coefficient de sensibilité γ ; CTN = R25 + coefficient B ; CTP (type KTY) = R25 + coefficient de température (%/°C). Curseur `simControl` pendant la simulation : éclairement 0→1000 lx / température −55→+125 °C. Schéma interne (bouton K) = symboles dessinés par Frank (`*-schema`), superposés tels quels (même viewBox que le dessin externe) ; catégorie « Discrets », libellés i18n FR.
1. ✅ Simulation avec résistances adjointes (item « la mesure prend en compte les résistances adjointes ») : les 3 composants sont des RÉSISTANCES dans la netlist (`kind: resistor`) dont la valeur suit la caractéristique `variableResistorOhms` (model.mts) — LDR R = R1lx·E^(−γ) (obscurité → 10 MΩ), CTN R = R25·e^(B(1/T−1/T25)), CTP R = R25·(1+tc/100·(T−25)). Nouvelle `adcDividerLevels` : chaque entrée ADC dont le réseau résistif contient une résistance variable suit le PONT DIVISEUR réel — plus courts chemins résistifs du nœud vers VCC et vers la masse SANS traverser le rail opposé (un rail est une source, pas un conducteur : `minOhmsPath` reçoit les nets interdits), tension = Rb/(Rh+Rb), un seul rail câblé → nœud tiré à ce rail. `resistiveGraph` factorisé (valeur en direct `liveOhms` + partId par arête) — les calculs LED voient donc AUSSI la résistance de repos d'une LDR/CTN/CTP en série. sim.mts repose les tensions sur chaque mouvement de curseur (`setAnalog`), paramètres relus depuis l'inspecteur.
1. ✅ Test de régression permanent `verify:divider` (ajouté à verify:all) : 32 contrôles — 10 caractéristiques R(x) (points remarquables + paramètres inspecteur), 9 ponts diviseurs (classique/inversé/CTN mi-tension/liveOhms/fixes seuls ignorés/un seul rail/rail non traversé/Pico GP26), 4 schémas internes, 9 rendus réels Chrome headless (dessins, tailles 1:1, curseur absent en édition / présent en simulation, événement `input`, plages, broches). Capture visuelle validée (pattes sur pastilles, symboles calés). typecheck + build + verify:all OK ; `.edit.svg` archivés dans `svg retouche/Validé/`.
# v2026.7.101
1. ✅ Physique de résistance série étendue aux afficheurs 7 segments et à la barre de LED (fin de l'item « valable pour tous les composants à résistance indispensable ») : `sevenSegSeriesOhms` (model.mts) — broche du segment vers sa source (ou son puits en anode commune) + MEILLEUR chemin d'un commun (COM.1/COM.2/COM ou DIGn multiplexé) vers masse/VCC ; `ledBarSeriesOhms` — anode An / cathode Cn de chacune des 10 LED. Un segment ou une LED de barre allumé en sur-courant (>35 mA, ex. sans résistance) GRILLE tout le composant : flamme animée superposée (même dessin que la LED), état définitif jusqu'au prochain lancement ; résistance trop forte → segment/LED atténué (valeur fractionnaire rendue par `color-mix(in srgb, allumé x%, éteint)` dans les forks) voire éteint. Calcul par BROCHE de segment (partagé entre chiffres, cache par frame) — s'applique aux deux chemins de rendu du 7 segments (1 chiffre anti-scintillement et multiplexé).
1. ✅ `verify:led` étendu à 28 contrôles : + 5 netlist (segment avec R/direct/non câblé, LED de barre avec R/direct) + 3 rendus headless (segment plein `red` / atténué 40 % en color-mix / éteint sombre ; flamme du 7 segments grillé ; barre plein/atténué/flamme) + capture visuelle validée. typecheck + build + verify:all OK.

# v2026.7.100
1. ✅ Physique de résistance série étendue à la LED RGB : nouveau `rgbSeriesOhms(diagram, partId, canal)` (model.mts, graphe résistif factorisé dans `resistiveGraph` partagé avec la LED simple) — cathode commune : canal ← source (MCU/VCC) + COM → masse ; anode commune : COM ← VCC + canal → puits (broche MCU tirée basse ou masse). Chaque canal a sa propre résistance et sa propre Vf (rouge 1,8 V / vert 2,1 V / bleu 3,0 V) : un canal en sur-courant (>35 mA, ex. câblé en direct) GRILLE toute la LED — flamme animée + corps carbonisé (filtre `grayscale`+`brightness` sur le dessin, la flamme hors du groupe garde ses couleurs) + halos éteints, définitif jusqu'au prochain lancement ; résistance trop forte → canal assombri voire éteint (facteur composé avec le duty PWM existant, y compris l'inversion anode commune).
2. ✅ `verify:led` étendu à 20 contrôles : + 4 netlist RGB (canal avec R 220, canal en direct, canal non câblé → null, anode commune via 5V/broche MCU) + 1 rendu headless (RGB grillée : flamme + groupe carbonisé + halo à 0) + capture visuelle validée. typecheck + build + verify:all OK. ⏳ 7 segments / barre de LED : reportés (item reformulé en tête de liste).

# v2026.7.99
1. ✅ LED et résistance série — physique réaliste (item « résistance trop faible → flamme, trop forte → moins de luminosité », volet LED simple) : nouveau calcul `ledSeriesOhms` (model.mts) — netlist SANS fusion des résistances (`buildNets(diagram, false)`), résistances = arêtes pondérées par leur valeur, plus court chemin (Dijkstra) broche source (MCU/VCC) → anode PLUS cathode → masse (fils/platines = courts-circuits ; 0 Ω = branchée en direct, null = circuit ouvert ; chemins parallèles : pire cas — chemin min — retenu). `ledElectrical` : I = (Vs − Vf)/R avec Vs = 5 V AVR / 3,3 V RP2040 et Vf par couleur (rouge 1,8 V… bleu 3,0 V, table `LED_FORWARD_V`) → sur-courant destructeur au-delà de 35 mA, pleine luminosité à partir de 10 mA, proportionnelle en dessous, éteinte sous 0,2 mA.
2. ✅ En simulation (sim.mts) : dès que la LED conduit (niveau haut OU PWM — le rapport cyclique ne protège pas du courant de crête), sur-courant → LED GRILLÉE : flamme animée superposée (vacillement CSS, dessinée dans le fork led-element), verre noirci `#3a3a3a`, halo éteint — état définitif jusqu'au prochain lancement (▶ ou ⟲ « remplacent » la LED, `burnedLeds` vidé au startRun). Résistance trop forte → `brightness` multipliée par le facteur de luminosité (10 kΩ ≈ 3 %, 20 kΩ → éteinte), composé avec le duty PWM existant.
3. ✅ Test de régression permanent `verify:led` (ajouté à verify:all) : 15 contrôles — 6 netlist résistive (série anode/cathode, 2 R en série, direct, ouvert, parallèle), 7 physique (14,5 mA nominal, 47 Ω grillée, direct grillée, 10 kΩ sombre, 20 kΩ éteinte, ouvert, bleue sur 3,3 V), 2 rendu réel en Chrome headless (saine : pas de flamme/verre coloré/halo allumé ; grillée : flamme + verre noirci + halo éteint) + capture visuelle validée. typecheck + build + verify:all OK. ⏳ Extension RGB/7 segments/barre LED reportée en tête de liste.

# v2026.7.98
1. ✅ Décalage des composants au lancement de la simulation (item « le capteur de son se décale ») : cause racine — le curseur `simControl` apparaissait DANS LE FLUX sous le dessin, la boîte du composant grandissait, et comme `applyRotation` (editor.mts) tourne `.part__body` autour de son CENTRE (`transform-origin: center center`), le centre déplacé décalait le dessin de tout composant TOURNÉ ou RETOURNÉ (mesuré avant correctif : son 90° → 32 px, DHT22 180° → 115 px, NTC 270° → 32 px, HC-SR04 45° → 10 px…). Correctif central dans `sim-control-styles.mts` (+ `dht22-element.mts`, styles locaux) : le contrôle est sorti du flux (`position: absolute; top: 100%` sous le dessin, `:host` relative) — couvre les 7 familles concernées, y compris les composants personnalisés (`custom-part` réutilise le même CSS). Même correctif de fond que la note de musique du buzzer (v2026.7.93).
2. ✅ Test de régression permanent `verify:simshift` (ajouté à verify:all) : banc reproduisant `applyRotation` en Chrome headless — 7 cas (son 0°/90°, DHT22 180°, HC-SR04 45°, pouls 90°, NTC 270°, flamme 90°), delta de position du dessin avant/après pose de l'attribut `simulating` exigé à (0,0) px ET contrôle bien affiché. Contre-épreuve faite sur le code d'avant correctif : le banc détecte bien les 6 décalages. typecheck + build + verify:all OK.

# v2026.7.97
1. ✅ Segments éteints des afficheurs 7 segments assombris pendant la simulation : les dessins retouchés portaient DÉJÀ les deux couleurs (attribut `fill="#444444"` sombre réaliste, écrasé par le `style` inline gris clair `#c0c0c0` d'édition — vérifié sur les 3 variantes 1/2/4 chiffres, points décimaux compris). Le composant mémorise les deux au premier passage (`dataset.off`/`offSim`) et bascule selon l'attribut `simulating` : en édition, gris clair lisible ; en simulation, un segment éteint est à peine visible sur le fond noir, comme un vrai afficheur (retour automatique au gris d'édition à l'arrêt). Au passage, `setLocked` (editor.mts) pose désormais l'attribut `simulating` sur TOUS les composants (plus seulement ceux à `simControl` — les composants qui ne le déclarent pas l'ignorent, `custom-part` garde sa garde `!this.control`, la classe z-index `part--sim-active` reste réservée aux simControl). Vérifié en Chrome headless (2 chiffres, 3 états) : édition rgb(192,192,192), simulation éteinte rgb(68,68,68), « 12 » allumé rouge vif sur segments sombres. typecheck + build + verify:all OK.

# v2026.7.96
1. ✅ Déformation du capteur de tilt refaite (item « diminution symétrique du côté gauche, extrémités des pattes fixes ») : l'ancienne solution « deux moitiés clipées + skewY » (v2026.7.69) déplaçait les bouts de pattes dessinés (~1,9 px — l'origine du skew était le bord du boîtier x=82.2, pas la ligne des extrémités x=100) et faisait se CHEVAUCHER les deux moitiés à la couture (déplacement du skew constant en y, doublement des motifs au centre). Remplacée par UNE homographie CSS `matrix3d` (trapèze keystone) sur l'élément `<svg>` racine — c'est un élément HTML, les transforms 3D s'y appliquent (l'échec `perspective`/`rotateY` de v2026.7.69 ne concernait que les `<g>` INTERNES du SVG). Constantes calées sur la grille lattice2 de `tilt-incline.svg` (référence de Frank) : seuls les 2 coins gauches rentrent de 5,0 px (haut ET bas, symétrique autour de y≈30), la verticale x=100 = extrémités des pattes vérifie w=1 → fixe au pixel exact ; déformation globale continue (le corps entier, pattes comprises, converge vers les extrémités ; la légère compression horizontale de la perspective fait visuellement basculer le boîtier). Un seul SVG rendu au lieu de deux. Vérifié en Chrome headless : bbox transformée exacte aux prédictions (±0,01 px), superposition plat/incliné zoom ×16 sur les 3 extrémités → aucun décalage au bout, trapèze symétrique confirmé en vue globale. typecheck + build + verify:all OK.

# v2026.7.95
1. ✅ Icône du traceur : `media/serialTracer.svg` (dessin de Frank) remplace l'emoji 📈 — bouton de la barre de simulation (`<img>` comme le moniteur série, classe `canvas-controls__btn--icon`) + en-tête du panneau traceur (nouvelle classe `.plotter__head-icon`, 14 px). Vérifié en capture Chrome headless : courbe orange + point terminal (marker `context-stroke`) rendus, lisible à 20 px sur fonds sombre et clair. typecheck + build + verify:plotter OK.

# v2026.7.94
1. ✅ Traceur de courbes intégré (item « visualisateur virtuel ou teleplot » — les 2 pistes explorées, outil maison retenu AVEC le protocole Teleplot comme format, zéro dépendance/hors-ligne ; un relais UDP opt-in vers l'extension Teleplot reste possible plus tard) : nouveau panneau 📈 sous le moniteur série (`plotter.mts`, canvas 2D maison). DEUX sources tracées ensemble : (a) télémétrie série au format Teleplot `>nom:valeur` (variantes `>nom:ts:valeur`, unité `§u`, drapeaux `|g` acceptées ; mêmes sketchs réutilisables tels quels avec le vrai Teleplot sur du vrai matériel) — ces lignes sont absorbées du moniteur série (retenue prudente : une ligne candidate non conforme ou jamais terminée est rendue à la console après 500 ms, l'invite REPL `>>>` passe intacte) ; (b) sondes internes : toute tension posée sur une broche analogique par un capteur (`setAnalog` : potentiomètre, LDR, gaz, pouls…) est tracée EN VOLTS (Vref 5 V AVR / 3,3 V RP2040) sans une ligne de code dans le sketch, en escalier (valeur tenue, points dédupliqués).
2. ✅ Traceur — interface : fenêtre glissante 5/10/30/60 s, échelle Y automatique (pas « ronds » 1-2-5), grille discrète, légende en puces cliquables (pastille couleur + nom + valeur courante ; clic = masquer/afficher la série), ⏸ gel d'affichage (la collecte continue), réticule de survol avec info-bulle des valeurs à l'instant pointé, courbes conservées à l'arrêt pour analyse, bouton 📈 de la barre d'outils + auto-ouverture à la première donnée (préférence mémorisée si fermé explicitement). Palette 8 teintes ordre fixe optimisé daltonisme, validée contre les fonds VS Code clair/sombre (jamais recyclée : 9e série et suivantes en gris), suit le thème en direct. i18n FR complète (webview + l10n hôte).
3. ✅ Traceur — export CSV : format long `temps_s;grandeur;valeur;unite`, séparateur « ; » et virgule décimale en français (ouverture directe dans Excel FR), boîte d'enregistrement VS Code (`exportCsv`/`saveCsv` dans panel.ts).
4. ✅ Test de régression permanent `verify:plotter` (ajouté à verify:all) : 17 contrôles en Chrome headless sur le vrai module bundlé — filtre série (ligne coupée en morceaux, virgule décimale, invite REPL intacte, valeur non numérique rendue, unité/drapeaux/horodatage Teleplot, retenue rendue après délai), escalier dédupliqué des sondes, auto-affichage unique, légende cliquable, CSV, pixels réellement peints sur le canvas. + capture visuelle dark/light (`view-plotter.mjs`) — a révélé et corrigé un vrai bug : `display:flex` du message « en attente » écrasait l'attribut `hidden`. typecheck + build + verify:all OK.

# v2026.7.93
1. ✅ Effet visuel appuyé/relâché du bouton 6 mm restauré : le SVG retouché (`button-6mm.svg`) portait un style inline cassé `fill:url(#grad-down-$%7BuniqueId%7D)` sur l'ellipse `.button-active-circle` — le placeholder `${uniqueId}` du composant Wokwi d'origine, URL-encodé par Inkscape et jamais substitué. Le style inline gagnait sur l'attribut `fill="url(#grad-down-pushbutton0)"` correct ; la référence invalide peignait l'ellipse « enfoncé » comme `none` → invisible. Style inline nettoyé (l'attribut correct s'applique). Vérifié en rendu Chrome headless : relâché = dégradé relief, appuyé = dégradé inversé, bascule dynamique press/release OK.
2. ✅ « Bon fichier affiché mais l'ancien qui se lance » (v2026.7.91-2 non résolu) : quand la référence `codeFile` d'un `.projix` ne se résolvait sur AUCUN candidat, le chip affichait quand même le nom (sans alerte, `hasCodeFile` passait à vrai) et ▶ retombait EN SILENCE sur l'éditeur actif — c'est-à-dire le fichier du PROJET PRÉCÉDENT, recompilé ou relancé du cache (`runCached`). Triple correctif : (a) le manifeste `.projix` mémorise désormais AUSSI le chemin absolu du fichier de code (`codeFileAbs`, dernier candidat de résolution — couvre la réouverture sur le même poste avec un autre workspace, cas typique de l'échec) ; (b) référence introuvable → chip en ALERTE jaune/rouge (nom affiché + bulle « introuvable ») + avertissement VS Code explicite, état conservé au rechargement de la webview ; (c) ▶ REFUSE de compiler l'éditeur actif tant qu'un projet a une référence en échec (message d'erreur invitant à choisir via le chip 📄) — plus jamais l'ancien fichier lancé sans le dire.
3. ✅ Bulles du chip 📄 enfin en français dans la webview : les clés utilisées par sim.mts (« click to change, double-click to open ») manquaient au dictionnaire FR de `i18n.mts` (la v2026.7.81 n'avait corrigé que le bundle l10n de l'extension) — retombaient sur l'anglais. Clés ajoutées (+ la clé « introuvable » du correctif 2), anciennes clés orphelines retirées.
4. ✅ Buzzer : pastilles enfin au bout des pattes — le fork rendait le dessin retouché (viewBox 50×60, convention 1:1 px de la grille) étiré à 64×75 px ET la note de musique animée occupait 8 px de flux au-dessus du dessin : les bouts de pattes tombaient à ~(26, 70) px quand les pastilles `pinInfo` étaient posées à (20,50)/(30,50). Rendu remis à l'échelle 1 (50×60), note sortie du flux (position absolue au-dessus du corps), SVG externe nettoyé (namedview/attributs Inkscape, groupes vides, ids génériques → `buzzer-*`) et bouts de pattes redessinés EXACTEMENT sur les croisements (20,50)/(30,50). Vérifié en rendu Chrome headless : points viewBox (20,50)/(30,50) → (20.00,50.00)/(30.00,50.00) px dans le repère de l'élément. typecheck + build + verify:all OK (13 suites).

# v2026.7.92
1. ✅ Audit de sécurité complet (extension + webview + scripts) : aucune faille exploitable trouvée — CSP webview stricte (script-src nonce seul, les SVG injectés par `innerHTML` ne peuvent pas exécuter de script), `.projix` jamais extrait sur disque (pas de zip-slip), compilation via `execFileSync` sans shell (pas d'injection de commande), `openExternal` restreint à docs.wokwi.com, firmware en HTTPS épinglé micropython.org. Trois durcissements appliqués : (a) nonce CSP généré par `crypto.randomBytes` au lieu de `Math.random` prédictible (panel.ts, help.ts) ; (b) pont réseau Pico W restreint aux protocoles http/https — un schéma `data:`/`file:`/autre est refusé avant le `fetch` de l'hôte (panel.ts) ; (c) évaluateur d'expressions `expr.mts` : résolution des fonctions/constantes par `Object.hasOwn` — les propriétés héritées du prototype (`constructor`, `toString`…) ne sont plus acceptées comme identifiants. typecheck + build + verify:creator + verify:net OK.
1. ✅ Vérification télémétrie : AUCUNE télémétrie dans le code ni les dépendances (avr8js, rp2040js, lit, jszip — toutes hors-ligne ; aucun machineId/TelemetryReporter/analytics/beacon). Seuls accès réseau, tous légitimes et déclenchés par l'utilisateur : téléchargement firmware micropython.org (sur demande), vérif MAJ libs registry.npmjs.org (opt-in, défaut désactivé), pont réseau Pico W (requêtes du script simulé de l'utilisateur, désactivable), liens externes GitHub/wokwi (clic). Rien à supprimer.

# v2026.7.91
1. ✅ LED embarquée du Pico W : plus de cercle sombre visible à l'arrêt — DEUX sources de « cercle noir » supprimées. (a) La pastille ajoutée (le rendu Fritzing du Pico W n'a pas de LED pilotable) était pilotée en COULEUR (`#3a4a3a` sombre + contour noir à l'éteint) au lieu de l'OPACITÉ comme le Pico (`#circle16` du dessin, invisible éteint) — unifié : même pastille verte `#8cff5a` pilotée en opacité (0 = rien, 1 = verte + halo), plus de mode `fill`, plus de contour (`pico-board.mts`). (b) Le dessin Fritzing lui-même imprime un point vert foncé en dur au centre de la LED (`#circle178`, rgb(86,176,57) — paraît noir à taille réelle, toujours visible même LED éteinte) : retiré au chargement du SVG. Vérifié en rendu Chrome headless (zoom ×10 sur la zone LED, avant/après + Pico en référence) : éteinte → rectangle clair nu comme la Pico, allumée → verte avec drop-shadow.
2. ✅ Ouverture de 2 `.projix` à la suite : le fichier de code du PREMIER projet restait actif (compilé/exécuté à la place de celui du second) quand la référence `codeFile` du second ne se résolvait pas (référence relative au workspace d'origine, ex. `7seg-pico.py` enregistré depuis un autre dossier ouvert). `restoreCodeFile` (panel.ts) oublie désormais TOUJOURS le fichier précédent avant de restaurer (`setCodeFile(undefined)` — mieux vaut aucun fichier que le mauvais), et résout les références relatives dans l'ordre : dossier du `.projix` lui-même (le code vit à côté du projet, convention testkablix), puis CHAQUE dossier du workspace (plus seulement le premier), puis le nom de fichier seul dans le dossier du `.projix`. typecheck + build + verify:all OK.

# v2026.7.90
1. ✅ Éditeur de composants — paramètres de définition : section « Paramètres du composant » (nom identifiant + libellé + valeur par défaut, bouton ＋/✕). Chaque paramètre devient un champ numérique de l'inspecteur (attr `prm_<nom>`, valeur par défaut posée à la création de l'instance) ET une constante utilisable par son nom dans la caractéristique du contrôle de simulation (ex. `R1lx` d'une LDR). Noms validés (identifiant, unicité) à l'enregistrement, bordure rouge en direct sur un nom invalide.
2. ✅ Éditeur de composants — contrôle de simulation : section « Contrôle de simulation » (Aucun / Curseur / Interrupteur). Curseur : libellé, unité, min/max/pas + caractéristique optionnelle ; interrupteur : libellé. Le contrôle apparaît SUR le composant pendant la simulation (même infra `simControl`/attribut `simulating`/`part--sim-active` que les capteurs intégrés, styles partagés `sim-control-styles`) : curseur → source analogique (AO), interrupteur ON/OFF → source numérique (OUT, état initial = attr `state` de l'inspecteur) ; le champ statique « Position (%) » / « State (0/1) » de l'inspecteur s'efface quand le contrôle pilote la même sortie. Relu en direct par le moteur sur l'événement `input` (sim.mts), comme la CTN intégrée.
3. ✅ Caractéristique = expression mathématique SÛRE (nouveau module `expr.mts`, parseur descente récursive maison — pas d'eval/Function, la CSP de la webview l'interdirait) : `+ - * / % ^` (puissance associative à droite), parenthèses, moins unaire, nombres (`3.3`, `.5`, `1e-3`), fonctions (abs, sqrt, exp, ln/log, log10, log2, floor, ceil, round, min, max, pow, clamp), constantes pi/e, variables = `x` (valeur du curseur) + paramètres du composant (relus en direct dans les attrs de l'inspecteur, modifiables par instance). Le résultat est la TENSION DE SORTIE EN VOLTS, normalisée par la Vref de la carte (5 V AVR, 3,3 V RP2040) et bornée 0..Vref ; expression vide = rampe linéaire min→max → 0→Vref. Identifiants inconnus et erreurs de syntaxe refusés à la compilation → validation en direct dans le créateur (message vert/rouge sous le champ, liste des variables disponibles). Exemple LDR : curseur « Éclairement » 0–1000 Lx, caractéristique `3.3*x/(x+R1lx)` avec paramètre `R1lx` (l'item « à faire » n° 2 — LDR/CTN/CTP intégrées avec vraies résistances — reste ouvert, cette infra en couvre le cas d'usage via un composant personnalisé).
4. ✅ Paramètres et contrôle embarqués dans l'export/import `.json` du composant (validation légère à l'import : noms d'identifiant, types slider/switch) et persistés avec le composant.
5. ✅ `verify:creator` étendu à 25 contrôles : les 15 marqueurs SVG existants + 10 sur l'évaluateur (priorités, associativité de `^`, moins unaire, caractéristique LDR exacte, fonctions, constantes, notation scientifique, rejet variable/fonction inconnue et syntaxe invalide). typecheck + build + verify:all OK (dont verify:debug du lot précédent).

# v2026.7.89
1. ✅ Variables locales (r, g, b…) enfin affichées en pas-à-pas MicroPython (ex-item « __kx_vars() ne lit que globals() ») : MicroPython n'offre AUCUNE introspection des frames à l'exécution (pas de `sys._getframe` ni `settrace` dans le firmware standard ; `locals()` y renvoie les globales — les locales sont compilées en slots anonymes). Solution : détection STATIQUE des locales de chaque `def` par l'instrumentation (`pydebug.ts` — paramètres, affectations simples/multiples/augmentées/annotées, cibles de `for`, `with/except/import … as`, `nonlocal` ; `global` exclu, `self`/`cls` masqués, corps de classe ignorés car invisibles depuis une lambda) ; chaque ligne instrumentée d'une fonction devient `__kx(N, lambda: [('r', lambda: r), …])` — la lambda n'est évaluée QU'EN PAUSE, un thunk qui lève NameError (variable pas encore affectée à cette ligne) est simplement ignoré, une locale écrase la globale de même nom dans le panneau (sémantique de la ligne courante). Préambule factorisé (`__kx_put` partagé globales/locales). Vérifié bout-en-bout sur firmware réel : arrêt dans une fonction → paramètres visibles, `b` absente AVANT son affectation puis `b = r + g` au pas suivant, globales toujours visibles. `verify-debug-py.mjs` étendu (7 nouveaux contrôles unitaires + 7 bout-en-bout) et enfin câblé dans la suite : `verify:debug`, inclus dans verify:all.

# v2026.7.88
1. ✅ Éditeur de composants refondu en plein écran (96 vw × 94 vh) : colonne formulaire (nom, modèle de simulation, rôles, liste des broches) + deux grandes zones maximisées côte à côte — vue externe et vue interne — avec zoom partagé (−/+/⛶ ajuster) ; l'ancienne textarea de code SVG est remplacée par un chargement de fichier.
2. ✅ Import du SVG externe par sélecteur de fichier avec détection automatique des marqueurs (nouveau module `svg-markers.mts`, rendu hors écran + `getScreenCTM` → gère unités mm d'Inkscape et transforms imbriqués) : cercle rouge pur rgb(255,0,0) opacité 0,8 = centre de broche ; texte rouge pur = nom de la broche la plus proche (devient l'infobulle de la pastille sur le canvas) ; cercle vert pur rgb(0,255,0) opacité 0,5 sur UNE broche = ancre de calage. Tous les marqueurs sont retirés du composant final ; le clic sur l'aperçu reste disponible pour poser des broches à la main (SVG sans marqueurs, anciens composants).
3. ✅ Vue interne (schéma) des composants personnalisés : second SVG importable, dessiné à la même échelle que l'externe et calé dessus par l'ancre verte (translation = ancre externe − ancre interne ; à défaut d'ancre, coins haut-gauche alignés) ; case « Superposer » dans l'éditeur pour contrôler le calage à l'œil ; affichée en simulation/édition par le bouton K comme les schémas internes intégrés (fond blanc translucide), persistée dans `CustomPartData` (`innerSvg`/`innerOffset` + ancres pour recalcul en cas de réimport partiel) et embarquée dans l'export/import `.json` du composant.
4. ✅ Modèles de simulation extensibles par fichier `.json` (bouton ⇪ à côté de la liste) : format documenté `{ "label": "…", "kind": "led", "pinRoles": { "A": "anode" }, "attrs": { } }` (objet seul ou tableau ; `kind` = un des modèles intégrés — un .json recombine rôles/attributs par défaut, il n'introduit pas de comportement nouveau). Préréglages listés sous « Modèles importés », persistés côté extension (`kablix.simModels`, nouveaux messages `simModels`/`saveSimModels`).
5. ✅ Test de régression permanent `verify:creator` (ajouté à verify:all) : 15 contrôles en Chrome headless sur le vrai module bundlé — détection des 3 broches + noms + ancre sur un SVG façon Inkscape (mm, groupe translaté), conversion mm→px, nettoyage des marqueurs sans toucher au dessin, calage externe/interne, non-détection sur un SVG sans marqueurs (rouge opaque conservé). typecheck + build + verify:all OK.

# v2026.7.87
1. ✅ Dossier `testkablix/` : recette de test exécutable — un test par composant du catalogue (32 composants) × 2 familles (Arduino Uno en C, Raspberry Pi Pico en MicroPython, sauf HC-SR04 = AVR seul) + un blink par carte de dev (uno, nano, mega, pico, picow) = 68 tests. Chaque test = programme + `.projix` prêt à ouvrir (schéma câblé + carte + référence au fichier de code) ; sketchs `.ino` dans un dossier par sketch (convention Arduino) avec le `.projix` à côté, scripts `.py` et leur `.projix` à la racine. Généré depuis une source de vérité unique `_spec.mjs` par `_generate.mjs` (ne jamais retoucher les fichiers générés à la main).
2. ✅ Vérification automatique `testkablix/_verify.mjs` (mode `--quick` sans toolchain) : 808 contrôles structurels des 68 `.projix` (archive, manifeste, types du catalogue, broches de chaque fil, câblage résolu par les MÊMES fonctions de bindings que la simulation — ledMcuPin, potBindings, spiDeviceBindings…) + compilation réelle des 35 `.ino` (arduino-cli, libs Adafruit SSD1306/ILI9341/NeoPixel, LiquidCrystal I2C, SD, DHT, Keypad, Servo installées) + syntaxe des 33 `.py` (py_compile) + bout-en-bout : blink-uno et blink-mega exécutés dans avr8js (LED D13), led-pico.py exécuté dans PicoEngine avec le vrai firmware MicroPython (LED GP15). Tout vert.

# v2026.7.86
1. ✅ NeoPixel sur Pico/MicroPython ENFIN fonctionnel (anneau, LED unique, matrice — ex-items 3-4-5) : la vraie cause racine n'était PAS la fidélité cycle-à-cycle de l'interpréteur ARM (hypothèse v2026.7.82) mais le cadencement de la boucle de lot de `KablixSimulator` (pico.mts) : `clock.tick()` n'était appelé qu'une fois PAR LOT (≤ 1 ms simulée), or `machine.bitstream` busy-wait sur SysTick (`SYST_CVR`, un Timer32 dérivé de `clock.nanos`) à 0,4-0,9 µs près — compteur gelé pendant tout le lot → chaque phase HAUTE durait un lot entier, toutes identiques (le symptôme observé en v2026.7.82). Correctif : `clock.tick()` à CHAQUE instruction (comme le Simulator amont) ; les alarmes dues en cours de lot (DMA, USB…) tombent aussi à l'heure exacte au lieu d'attendre la fin du lot. Aucun coût mesurable (verify:micropython toujours à 0,6 s). Vérifié bout-en-bout avec le module `neopixel` officiel du firmware réel : 4 couleurs décodées exactement par `Ws2812Decoder`. Test de régression permanent : `verify:neopixel`.
2. ✅ TFT ILI9341 / SPI multi-octets sur Pico/MicroPython fonctionnel (ex-item 6) : cause racine DANS rp2040js — `SimulationClock.linkAlarm` insérait les alarmes à échéance égale en LIFO ; le canal DMA TX qui se re-programme à l'instant courant repassait donc systématiquement devant l'alarme du canal RX en attente, l'affamant : rxFIFO SPI (8 entrées) en overrun, 120/128 octets rx perdus, `transCount` RX jamais à 0 → le firmware bloquait pour toujours sur `dma_channel_is_busy` (`machine.SPI` ≥ 32 octets = chemin DMA du firmware ; 1-4 octets passaient par le chemin pollé — le repro précise ainsi le constat v2026.7.84). Correctif : insertion FIFO des alarmes simultanées (conforme à l'arbitrage round-robin du vrai DMA), patché dans `node_modules/rp2040js` et figé via patch-package (`patches/rp2040js+1.3.2.patch`, en plus du patch PIO v2026.7.82). Vérifié : repro registres bruts sans firmware (canaux TX/RX finis, zéro overrun) + bout-en-bout firmware réel (écritures 1 + 4 + 128 octets : 133 transferts exacts en 0,6 s au lieu d'un blocage infini). Tests de régression permanents : `verify:spi` (unitaire + firmware).
3. ✅ typecheck + build + verify:all OK (dont verify:micropython, sensible au cadencement temps réel — 0,6 s inchangé).

# v2026.7.85
1. ℹ️ Vérification croisée AVR/avr8js pour les items 3-4-5-6 : écrit et exécuté un sketch Arduino Uno réel (compilé avec la vraie toolchain avr-gcc, exécuté sur `AvrEngine`) pilotant `Ili9341Device` en SPI multi-octets (CASET 4 octets + RASET 4 octets + RAMWR 128 octets d'affilée) — fonctionne parfaitement (99 transferts, pixel décodé correctement), aucun blocage contrairement à rp2040js. Idem pour un bit-bang WS2812 manuel (asm inline, timings HAUT/BAS 16 MHz déroulés sans boucle) piloté via `AvrEngine.setNeopixels()` — décodé correctement par `Ws2812Decoder` (fronts et ratios HAUT/BAS conformes). Confirme que les 4 items du backlog liés à rp2040js sont bien des limitations de fidélité cycle-exacte de rp2040js (SPI/DMA et bit-bang SIO), pas des bugs Kablix — la voie de contournement Pico (PIO officiel plutôt que bit-bang logiciel) reste à explorer mais hors de portée immédiate. Scripts de test jetables (non conservés dans le repo).

# v2026.7.84
1. ℹ️ TFT ILI9341 (SPI) sur Pico/MicroPython — investigation approfondie, bug non résolu, limitation de rp2040js identifiée : le décodeur `Ili9341Device` lui-même est correct (protocole CASET/RASET/RAMWR décodé exactement, testé et vert en isolation via `verify:i2c`), le câblage catalogue/moteur/rendu (`kind: 'spi-tft'`) est complet et fonctionnel (D/C et CS échantillonnés à chaque octet, comme pour l'OLED SPI). Le blocage vient du SPI matériel simulé de rp2040js : un premier octet de commande simple passe bien (`spi.write(bytes([c]))`, 1 octet), mais dès qu'un buffer de plusieurs octets consécutifs est écrit d'affilée (`spi.write(bytes(data))`, ex. les 4 octets d'un CASET/RASET, ou les 128 octets d'un remplissage RAMWR), le firmware MicroPython reste bloqué indéfiniment — confirmé reproductible avec un vrai firmware Pico et un script MicroPython pilotant `machine.SPI` en registres bruts (sans lib externe), le device recevait bien un flux `0xff` continu jamais interrompu au lieu de s'arrêter après le nombre d'octets attendu. Cause probable : synchronisation TX/RX FIFO ou DMA du contrôleur SPI simulé (`RPSPI`/`RPDMA`, rp2040js) qui ne signale jamais correctement la fin de transfert au firmware en environnement non temps-réel — même famille de limitation cycle-inexacte que le bug NeoPixel (v2026.7.82). Correctif hors de portée immédiate (chantier DMA/SPI rp2040js). Reporté ; test Arduino/AVR (avr8js, moteur SPI différent, potentiellement fonctionnel) pas encore fait.

# v2026.7.83
1. ✅ VSS de NeoPixel/LCD1602 = GND : le fil restait d'une couleur ordinaire au lieu de noir. Cause : `pinElectricalRole` (catalog.mts) ne reconnaissait GND que via le préfixe `GND`, jamais `VSS` (alias standard, utilisé par ces 2 composants). Regex étendue à `^(GND|VSS)`. Broche `DIN` du NeoPixel corrigée au passage (déclarait par erreur `signals: [GND()]`, copié-collé sur VSS — sans impact fonctionnel, `signals` n'est lu nulle part dans Kablix, mais trompeur).
2. ✅ OLED SSD1306 « ne marche pas en I²C » : le composant Kablix n'existait qu'en variante SPI (broches DATA/CLK/DC/CS), alors que le SVG dessine le vrai module combo 8 broches (SDA/SCL/SA0/RST/CS/VDD/VIN/GND) et que le moteur avait déjà tout le code I²C prêt (`kind: 'i2c-oled'`, `Ssd1306Device`) mais **orphelin** — aucune entrée de catalogue ne l'utilisait. Câbler SDA/SCL sur un bus I²C ne produisait donc jamais de trafic reconnu (le moteur attendait du SPI sur ces broches) : écran vide. Ajout d'un attribut `pins` (i2c/spi, pattern déjà utilisé par LCD1602) sur `kablix-ssd1306` : bascule les noms/rôles de `pinInfo` (mêmes positions, même dessin) sans dupliquer le composant ; `i2c-oled` par défaut. `spiDeviceBindings`/`buildI2cDevices`/rendu (`sim.mts`) adaptés pour router selon `attrs.pins`. Vérifié bout-en-bout avec le vrai driver MicroPython officiel `ssd1306.py` (adressage horizontal 0x21/0x22, style Adafruit) rejoué sur `PicoEngine` : `fill_rect`/`pixel`/`show()` remplissent bien le tampon GDDRAM attendu. typecheck + build + verify:all OK.

# v2026.7.82
1. ℹ️ NeoPixel (`machine.bitstream`) — investigation approfondie, bug non résolu, limitation de rp2040js identifiée : `machine.bitstream()` de MicroPython pilote la broche en **SIO** (bit-bang logiciel cycle-compté), jamais en PIO. L'interpréteur ARM Thumb de rp2040js ne compte pas les cycles par instruction de façon fidèle au silicium pour cette routine précise : toutes les durées HAUT mesurées sont identiques au lieu de varier bit 0/bit 1 (confirmé sur firmware v1.20.0 et v1.28.0). Corriger ça = audit cycle-exact de l'émulateur ARM — chantier profond et risqué (impact potentiel sur tout le timing CPU). Reporté.
2. ✅ Bug réel trouvé et corrigé au passage (rp2040js, `node_modules`, patché via patch-package — `patches/rp2040js+1.3.2.patch`) : le diviseur d'horloge PIO (`SMx_CLKDIV`) n'était jamais appliqué (`clkDivRestart()` stub non implémenté), et le PIO tournait en boucle `setTimeout(0)` autonome déconnectée du temps simulé (même famille de bug que l'USB-CDC v2026.7.65). Corrigé : accumulateur fractionnaire honorant CLKDIV + `advance(sysCycles)` piloté par l'hôte (`pico.mts`, en phase avec `core.executeInstruction()` et les sauts WFE). Ne corrige pas le bug NeoPixel (item 1, cause différente) mais bénéficiera à tout futur usage du PIO. typecheck + build + verify:all OK, aucune régression (dont verify:micropython, sensible au cadencement temps réel).

# v2026.7.81
1. ✅ Chip fichier de code (v2026.7.78, retour Frank « Hallucination ? ») : DEUX bugs distincts trouvés (le code existait bien). (a) `sim.mts` réécrivait dynamiquement `codeFileBtn.title` avec d'anciennes clés anglaises différentes de celles traduites dans `panel.ts` → bulle toujours en anglais. Clé unifiée + ajoutée au bundle FR. (b) Séquence DOM `click`→`click`→`dblclick` : le simple clic (ouvre le sélecteur de fichier) s'exécutait systématiquement avant que le double-clic ne soit détecté, empêchant `openCodeFile` de jamais s'exécuter en pratique. `pickCodeFile` différé de 250 ms (fenêtre standard de double-clic), annulé si un `dblclick` survient entre-temps.
2. ✅ Point d'extrémité de fil décalé (v2026.7.76) : bug de `box-sizing` — la bordure blanche 1px ajoutée en v2026.7.76 s'additionnait à `width`/`height` (`content-box` par défaut) sans recalcul du margin de centrage négatif, décalant la pastille visible en bas-à-droite du vrai point de connexion. `.wire-endpoint` passé en `border-box` avec dimensions/margin recalculés.
3. ✅ Lisibilité des textes de curseur de simulation (v2026.7.74) : fond blanc semi-transparent (`rgba(255,255,255,0.75)`) ajouté sur `label`/`.val` dans le style partagé `sim-control-styles.mts` — s'applique automatiquement aux 5+ composants qui l'utilisent (hc-sr04, dht22, pouls, NTC, flamme/gaz/son/lumière).
4. ✅ Bulle PIR (v2026.7.70) : suit désormais le curseur de la souris (position calculée à chaque `pointermove`, relative au composant) au lieu de rester plaquée en haut ; n'apparaît plus qu'au survol effectif ou en état `sticky` (verrouillé), suppression de l'ancien 3ᵉ état affiché en permanence dès le passage en simulation.
5. ✅ Déformation tilt-switch (v2026.7.69) : le resserrement du bord gauche n'apparaissait qu'en haut. Diagnostic géométrique sur `tilt-incline.svg` (référence Frank) : trapèze symétrique (haut ET bas se resserrent vers le centre, bord droit fixe) — pas exprimable par un `matrix()` 2D unique (skew ne penche que d'un côté). `perspective`+`rotateY` CSS essayé mais Chrome ne rend pas les `<g>` SVG en 3D (confirmé en headless : transform reste l'identité). Solution retenue : SVG dupliqué en deux moitiés superposées (clip-path haut/bas), chacune avec son propre `skewY` ancré sur le bord droit fixe — trapèze symétrique confirmé par capture d'écran headless. Comportement changé en même temps : MAINTENIR le clic incline (au lieu de bascule immédiate), RELÂCHER annule (retour à plat), Ctrl+clic verrouille en permanence (`sticky`, inchangé) — `onClick` remplacé par `onPointerDown`/`onWindowPointerUp` (écoute sur `window` pour capter le relâchement même hors composant). typecheck + build + verify:all OK.

# v2026.7.80
1. ✅ DHT22 en MicroPython/Pico : `dht.DHT22(Pin(13)).measure()` levait une exception (timeout/checksum). Cause : `setDht22` — qui simule les réponses du capteur sur la broche de données (protocole 1-wire : signal de départ détecté puis accusé + 40 bits programmés en temps simulé) — n'existait QUE côté moteur AVR (`avr.mts`) ; `PicoEngine` n'implémentait pas du tout cette méthode optionnelle, donc `sim.mts` l'ignorait en silence et la broche GPIO ne bougeait jamais — le vrai module Python `dht` (natif au firmware) attendait indéfiniment une réponse d'un capteur absent. Portée l'implémentation dans `pico.mts`, même principe que le HC-SR04 (v2026.7.71) : détection du front sur la broche + réponse programmée en nanosecondes horloge RP2040 via `sim.onTick`/`scheduled` (réutilise `dht22.mts`, déjà agnostique du moteur). Vérifié bout-en-bout (vrai firmware MicroPython, `import dht` réel) : `d.measure()` puis `d.temperature()`/`d.humidity()` renvoient exactement les valeurs simulées (23.5 °C / 55.2 %). typecheck + build + verify:micropython OK.

# v2026.7.79
1. ✅ Chargement d'un projet .projix pendant une simulation en cours : le moteur continuait de tourner sur l'ancien schéma pendant que l'éditeur affichait déjà le nouveau. `loadProject` (sim.mts) appelle désormais `stopRun()` avant de charger le nouveau schéma/carte — même fonction que le bouton ■ (coupe le moteur, réinitialise les visuels, déverrouille l'édition, masque le bandeau simulation). typecheck + build OK.

# v2026.7.78
1. ✅ Double-clic sur le nom du fichier de simulation (chip 📄 de la barre d'outils) : ouvre désormais ce fichier dans l'éditeur (volet de gauche, `ViewColumn.One`), au lieu de rouvrir la boîte de dialogue « choisir un fichier » (réservée au clic simple). Nouveau message webview→extension `openCodeFile` (sim.mts → panel.ts `openCodeFile()`, réutilise le pattern déjà en place pour `showDebugLine`). Bulle d'aide mise à jour (« cliquer pour changer, double-cliquer pour ouvrir ») + traduction FR. verify:all + typecheck OK.

# v2026.7.77
1. ✅ Bulle du bouton REPL non traduite : `l10n.t('Start an interactive MicroPython REPL (no script)')` était déjà en place côté code (panel.ts:1004) mais la clé était absente de `l10n/bundle.l10n.fr.json` → retombait sur le texte anglais brut. Ajoutée (« Démarrer un REPL MicroPython interactif (sans script) »). Vérifié : c'était la seule clé `l10n.t(...)` de panel.ts manquante au bundle FR (62 clés utilisées, 1 manquante).

# v2026.7.76
1. ✅ Point de 2 px aux extrémités d'un fil sélectionné (signale qu'on peut débrancher) : le point existait bien dans le DOM (`.wire-endpoint`, `buildEndpointHandles`, editor.mts — code jamais cassé depuis son introduction v2026.6.29) mais avait perdu toute bordure/ombre en v2026.6.40 (réduit de 12px à 2px « discret »), le laissant visuellement invisible sur un fil de couleur proche (bleu/vert dupont) — d'où l'impression de disparition totale. Ajout d'une bordure blanche 1px + ombre portée (media/styles.css, `.wire-endpoint`), même traitement que `.wire-handle` (poignées de coude, déjà contrastées « sur n'importe quel fond »). Taille du point inchangée (2px), zone de clic élargie (`::after`) inchangée. Vérifié : `buildHandles`/`buildEndpointHandles` créent bien les 2 poignées en rendu réel (composants + editor bundlés ensemble) avec le nouveau style calculé (bordure blanche, box-shadow noire, 2px, z-index 8) — la capture visuelle headless synthétique n'a pas pu confirmer le rendu final des web components (Lit ne s'affiche pas dans cet environnement de test), à confirmer en usage réel.

# v2026.7.75
1. ✅ Bandeau « ⚠ Simulation en cours : édition désactivée » : était centré à la même hauteur (top:8px) que les 2 barres d'outils du canvas (« entre » elles), ce qui le faisait apparaitre à côté plutôt que rattaché à la barre de simulation. Repositionné en dessous de la barre de simulation (gauche) : `top: 46px` au lieu de `8px` (media/styles.css, `.sim-banner`). Vérifié en rendu Chrome headless : aucun chevauchement avec les boutons.

# v2026.7.74
1. ✅ Les éléments de simulation (curseur/bouton d'un capteur) doivent apparaitre par-dessus tout : en édition, les fils passent volontairement au-dessus des composants (z-index 5 vs 3, pour rester lisibles) — en simulation, cela pouvait cacher le curseur d'un capteur traversé par un fil, ou un composant voisin posé après lui dans le schéma pouvait le recouvrir. `setLocked()` (editor.mts) ajoute désormais la classe `part--sim-active` (z-index 60) sur tout composant à contrôle de simulation (`simControl`) pendant que la simulation tourne. Vérifié en rendu Chrome headless (capteur de pouls chevauché par un fil + un HC-SR04 voisin) : le curseur reste au premier plan.

# v2026.7.73
1. ✅ Capteur de pouls (heart-beat) : signal simulé irréaliste — ligne de base quasi nulle (0,08) avec un pic très étroit atteignant presque le plein échelle. Un vrai capteur KY-039 varie peu en valeur absolue (bruit + faible modulation autour d'une ligne de base élevée) ; avec une ligne de base proche de zéro, l'algorithme de détection par seuil relatif classique (`max_value -= 1000 // delay_msec`, tuto KY-039 fourni par Frank) perd le pic en 1-2 échantillons à 60 ms d'échantillonnage et redéclenche sur la même descente → BPM mesuré ~2× trop élevé (ex. 72 BPM réglé → ~140-166 BPM mesurés). Vérifié bout-en-bout avec le vrai script Python de Frank (`heartbeat_detected`) rejoué sur PicoEngine : ligne de base relevée à 0,6 + amplitude 0,15 + pic élargi (0,05→0,10) → BPM mesuré correct sur la plage 45-100 BPM (majorité des mesures exactes ±1), dégradation progressive au-delà de 110 BPM par nature de l'algo (pas d'échantillonnage fixe 60 ms trop grossier à haute fréquence — limite de l'algo, pas de la simulation). verify:all OK.
2. ✅ (déjà livré en v2026.7.72) Curseur du capteur de pouls resserré à la largeur du composant — confirmé toujours correct après ce lot.

# v2026.7.72
1. ✅ Curseurs de simulation trop gros/longs, texte trop loin : style partagé `utils/sim-control-styles.mts` (min-width du curseur 90→44px, gap texte↔curseur 4→2px, police 11→10px) appliqué à hc-sr04, dht22, capteur de pouls, NTC température et à la base commune flamme/gaz/son/lumière (`analog-digital-sensor.mts`) — un seul point de vérité au lieu de 5 copiés-collés divergents. Modificateur `.val--wide` (46px) gardé pour les valeurs longues (bpm, °C négatifs) afin de ne rien tronquer. Vérifié en rendu Chrome headless (hc-sr04 3 chiffres, bpm 180, dht22 humidité/température négative) : rien de coupé ni chevauché.

# v2026.7.71
1. ✅ Capteur à ultrason (HC-SR04) ne marchait pas sur Pico/MicroPython : `PicoEngine` n'implémentait pas du tout `setUltrasonic` (méthode optionnelle du moteur, ignorée en silence par `sim.mts`) — TRIG/ECHO n'était câblé que côté AVR (Uno/Mega). Ajout de l'implémentation Pico : ECHO programmé en TEMPS SIMULÉ (nanosecondes horloge RP2040) et non via `setTimeout` réel (un timer JS de 0,2 ms peut se déclencher après des dizaines de ms simulées vu le cadencement temps réel du simulateur — l'écho arrivait hors fenêtre d'attente du firmware). Bug additionnel corrigé au passage dans `KablixSimulator.execute()` : le budget d'un lot d'instructions était figé en début de lot et pouvait sauter par-dessus une échéance programmée en cours de lot (front TRIG survenant à mi-lot) — recalculé à chaque instruction désormais. Vérifié bout-en-bout (vrai firmware MicroPython, TRIG/ECHO réel) : largeur ECHO mesurée = 1160 µs pour 20 cm (exact, 58 µs/cm). verify:all OK.

# v2026.7.70
1. ✅ PIR : la bulle « Détecte les mouvements de la souris » prend l'apparence d'une bulle native du navigateur (jaune pâle, bordure grise, coins carrés) — distincte des 2 autres bulles (fond sombre, inchangées).

# v2026.7.69
1. ✅ Capteur d'inclinaison — refonte complète (item « ne provoque une inclinaison que quand incliné puis revient... »). Bouton « Incliner »/« Incliné » SUPPRIMÉ : un simple CLIC sur le composant EN SIMULATION bascule l'état immédiatement (tout ou rien). Ctrl+clic = maintien permanent (`sticky`, même pattern que le PIR) ; recliquer normalement annule le maintien. Bulle contextuelle au survol : « Cliquer pour incliner / Ctrl + clic pour maintenir incliné » (→ « ...pour arrêter le maintien » si déjà maintenu).
2. ✅ Déformation visuelle : ROTATION CSS remplacée par une bascule TRAPÉZOÏDALE (`transform: matrix()` approchant le lattice2 de `tilt-incline.svg` fourni par Frank — bord droit fixe, bord gauche resserré ~5px haut/bas). `tilt.svg` restructuré : le corps est isolé dans deux groupes `.tilt-shape` (déformables), les 3 groupes de broches (g71/g70/g69) restent HORS de ces groupes → jamais déformés. Vérifié en Chrome headless : delta broches avant/après bascule = (0.000,0.000) px exact sur les 3 pattes ; capture visuelle confirmant le trapèze.

# v2026.7.68
1. ✅ Bandeau simulation : « ⚠ Simulation en cours » → « ⚠ Simulation en cours : édition désactivée » (fr + clé i18n renommée dans `i18n.mts`).
2. ✅ Bulle du bouton ☢ (K, bascule câblage interne/brochage) traduite en français : ajout des 2 clés manquantes dans `l10n/bundle.l10n.fr.json` (bundle VS Code natif, distinct du dict webview) — restaient affichées en anglais faute d'entrée.
3. ✅ Changement de propriété d'attribut (ex. K/A commune) dynamique si le câblage interne est affiché : `editor.mts` régénère les bulles de pastille et redessine le câblage interne (`renderInternalWiring`) immédiatement sur `attr === 'common'`, au lieu de rester figé sur l'ancienne polarité.
4. ✅ Changement de taille (afficheur 2↔4 digits, colonnes clavier) alors que le câblage interne / brochage est affiché : `scheduleSettle()` redessine `renderInternalWiring`/`renderPinout` une fois le dessin externe stabilisé (au lieu de rester à l'ancienne taille).
5. ✅ Texte des broches du clavier trop épais : `stroke:none` ajouté aux 12 `<text>` chiffres/symboles (1-9,*,0,#) des SVG `keypad-3col`/`keypad-4col` (n'avaient que `stroke-width` sans `stroke:none`, contrairement aux lettres A-D) + `font-weight: 300` en CSS shadow DOM.
6. ✅ PIR : détection de mouvement réel (pas juste présence) avec tolérance 400 ms — `hovering` s'arme sur un déplacement de souris effectif et retombe après un délai de grâce (`MOTION_GRACE_MS`) sans mouvement, au lieu de suivre l'entrée/sortie du survol. Bulle contextuelle à 3 états : « Ctrl+clic pour un mouvement permanent » (simulation, hors survol actif) → « Détecte les mouvements de la souris » (mouvement en cours) → « Mouvement permanent (Ctrl+clic pour arrêter) » (sticky, toggle Ctrl+clic).
7. ✅ Pinout mega : retouche z-order de Frank réimportée (SVG déjà remplacé, importé tel quel par `pinout.mts` — aucun pipeline de génération à relancer).

# v2026.7.67
1. ✅ Posters de brochage nano/uno/mega : les 3 sont posés en surimpression **sans déformation** (fini l'étirement vertical `k` qui décalait uno et mega). Chaque poster porte des pastilles de calage aux positions exactes des pins ; pose via la transform mesurée `coord_carte = s·coord_poster + t` (échelle uniforme : nano/uno s=1, mega s≈3.78). Transforms mesurées au navigateur (Chrome headless, `getBoundingClientRect` des pastilles vs `pinInfo` du composant, régression, erreur max 0,15 u sous-pixel).
2. ✅ Nano réactivé (était désactivé). Alignement des 3 vérifié visuellement (rendu headless carte externe + poster superposés) : rangées haute/basse + blocs ICSP/digital pile sur les pins.
3. ✅ Pastilles rouges (`#ee0000`/`#aa0000`) + numéros de pin rouges (`#aa0000`) — repères de calage de Frank — retirés des 3 SVG posters (36 nano, 31 uno, 85 mega). Zéro rouge restant.
4. ✅ `pinout.mts` : deux modes de pose explicites — `stretch` (pico/picow, inchangé) et `align` (nano/uno/mega). `renderPinout` gère les deux. typecheck + build OK.

# v2026.7.66
1. ✅ Servo au degré près (item « aucune rotation pour 10-40°, 60→45°, 140→180° ») : le mapping impulsion→angle était figé sur 1000-2000 µs — tout programme SG90 en 500-2500 µs (dont celui de Frank) voyait ses angles écrasés/clampés. Nouvelles propriétés d'inspecteur « Impulsion à 0° (µs) » / « Impulsion à 180° (µs) », défaut 500/2500 (datasheet SG90) ; régler 544/2400 pour la lib Servo Arduino. Interpolation linéaire entre les deux.
2. ✅ Précision vérifiée moteur réel (UF2 v1.28, PWM 50 Hz) : angles 0/1/10/33/45/90/135/170/179/180° → écart max +0,05°. verify:all OK.

# v2026.7.65
1. ✅ Simulation Pico 4× trop lente (programme servo de Frank : sleep(0.5) durait ~2 s) — cause racine : quand MicroPython arme le endpoint USB OUT du CDC sans données côté hôte, rp2040js répondait « transfert vide » en 10 µs et TinyUSB réarmait aussitôt → une IRQ toutes les ~25 µs simulées qui avortait chaque WFE. time.sleep() était une boucle chaude (20 000 réveils par 0,5 s). Correctif : réponse à la cadence d'un vrai hôte USB full-speed (1 ms) → 500 réveils, le firmware dort vraiment.
2. ✅ Cadencement TEMPS RÉEL du simulateur Pico (KablixSimulator) : ancre temps réel ↔ temps simulé. Sans elle, les sleep (désormais de vrais sauts d'horloge) s'écouleraient quasi instantanément ; avec elle, sleep(0.5) dure ~0,5 s réelle (mesuré 0,47-0,51 s par pas sur le balayage servo). Le code calculatoire reste sous le temps réel (plafond interpréteur rp2040js) : retard > 50 ms → ré-ancrage sans dette (pas de rattrapage qui escamoterait les sleep suivants).
3. ✅ `core.cycles` avance maintenant pendant les sauts WFE (bump AVANT le tick d'horloge) : la mesure d'impulsions servo/PWM/WS2812 (horodatée en cycles) reste juste PENDANT les sleep — vérifié : impulsions 500→2500 µs mesurées pendant le balayage 0→180°. Bonus : verify:micropython passe à 0,6 s ; le REPL inactif ne brûle plus 100 % CPU. verify:all OK.

# v2026.7.64
1. ✅ Servo : les 3 palonniers dessinés par Frank (horn-single = 1 branche, horn-double = 2, horn-cross = 4) sont intégrés ; feuille figée à 160×140 (lue dans le SVG). Chaque forme tourne autour de l'axe (calé sur le rond central via le translate Inkscape). Rendu headless validé (3 formes × 0/90/180°).
1. ✅ Broches recalées : Frank a déplacé le groupe `pins` (translate −9,5 ; −20,25) → pinInfo passé à GND(10,60) / V+(10,70) / PWM(10,80), arrondi sur la grille 10 px.

# v2026.7.63
1. ✅ Servo : passage à TROIS palonniers dessinés à la main (`horn-single/double/cross`) au lieu de la duplication par le code. Les 3 groupes sont pré-remplis avec une copie du bras actuel de Frank (à compléter pour double/cross, cf. à faire n°3). Le composant affiche le groupe choisi et le tourne autour de l'axe selon l'angle simulé ; la taille de feuille (viewBox) reste lue dans le fichier (ajustable). Rendu headless validé (3 formes × angles).

# v2026.7.62
1. ✅ Servo : dessin de Frank intégré (servo.edit.svg retouché — corps bleu, palonnier réaliste dans `horn-arm`, axe `axis` posé sur le rond central). Le composant lit l'axe EN TENANT COMPTE du `translate` qu'Inkscape ajoute quand on déplace le groupe (readAxis corrigé). single/double/cross dupliquent le bras de Frank autour de l'axe ; rotation validée headless. Broches pinInfo inchangées (x=20, y=80/90/100 — non déplacées par Frank).

# v2026.7.61
1. ✅ Servo — structure « UN SEUL bras » : le .edit.svg ne contient plus qu'un bras (`horn-arm`, vers le haut) + un marqueur d'axe (`axis`, croix magenta). Le composant DUPLIQUE ce bras en 1/2/4 branches (single/double/cross) et tourne l'ensemble autour de l'axe LU dans le fichier. Frank ne retouche donc qu'un bras + l'axe. Feuille agrandie 180×180 (marge pour rotation complète, réduisible par Frank).
2. ✅ Le composant lit la TAILLE DE FEUILLE (viewBox/width/height) et l'AXE directement dans servo.edit.svg → Frank peut ajuster la feuille et déplacer l'axe sans toucher au code. Rendu headless validé (single 1 bras, double 2, cross 4 ; rotation 0/45/90/180/270° autour de l'axe).

# v2026.7.60
1. ✅ Message « Simulation en cours » revu (item « le message sur la souris ne va pas ») : bandeau PERMANENT rouge sur jaune, centré en haut du canvas ENTRE les deux barres d'outils (`#sim-banner`, même hauteur top:8px), visible pendant toute la simulation. Clignote 3× (inversion rouge↔jaune) sur tentative d'édition interdite (`onBlockedEdit`).
2. ✅ Supprimés : l'ancien message flottant qui suivait le curseur (v57) ET le bandeau d'avertissement de la palette (`showLockWarning` + propriété `lockWarning` retirés d'editor.mts, plus réinséré au rebuild de la palette).

# v2026.7.59
1. ✅ Servo : dessin sorti dans `externe/servo.edit.svg` (RETOUCHABLE par Frank), un seul fichier « tout compris » — groupes `body` + `horn-single/double/cross` + repères `grid`/`pins` (pastilles pin-GND/V+/PWM). Boîte agrandie 170×125 conservée. AXE (moyeu) RECENTRÉ au centre de la boîte (85 ; 62.5) : corps + palonniers décalés en conséquence. Généré par `scripts/make-servo-edit.mjs`.
2. ✅ Le composant lit désormais servo.edit.svg (fin du bras procédural) : extraction des groupes (comptage de profondeur `<g>`, `<g/>` auto-fermants et commentaires gérés), affiche body + le bon horn-* et le fait tourner autour de l'axe. Rendu Chrome headless validé (3 formes × 5 angles : corps visible, axe centré, rotation OK). NB : broches à recaler dans le .edit.svg (cf. à faire n°3).

# v2026.7.58
1. ✅ LED en PWM : ne clignote plus, elle VARIE en luminosité (item « en pwm la LED clignote »). Cause trouvée sans le programme : le rendu de la LED simple (case 'led') lisait le niveau INSTANTANÉ (`ledOn`, digital) — en PWM il alterne 0/1 à haute fréquence → clignotement au rythme du rafraîchissement. Correctif : si l'anode est pilotée par une broche MCU en PWM (`ledMcuPin` + `pulseActive`), on lit le rapport cyclique (`readPwmDuty`) et on le mappe sur `brightness` (halo plus ou moins opaque), comme la LED RGB et le 7 segments. Sinon, comportement digital inchangé.

# v2026.7.57
1. ✅ Message flottant « ⚠ Simulation en cours » : affiché près du curseur pendant toute la simulation (position fixe, suit la souris) et clignote 3× en rouge quand une action d'édition INTERDITE est tentée (clic gauche pour déplacer un composant passif, ou Suppr/Backspace sur une sélection — pendant le verrouillage). L'éditeur expose `onBlockedEdit` (appelé aux points de blocage : body pointerdown non-interactif, touche Suppr verrouillée) ; sim.mts gère le toast (`.sim-toast` / animation `.sim-toast--blink`). Ancrage « près souris » = choix de Frank. Déclenchement validé headless.

# v2026.7.56
1. ✅ Bouton ☢ (K) déplacé du composant vers la BARRE D'OUTILS droite (en haut à gauche, avant l'autoroutage). Il n'apparaît que lorsque le composant SÉLECTIONNÉ dispose d'un câblage interne ou d'un poster de brochage (`onSelectionChange` → `hidden`), et agit sur ce composant (`toggleSelectedSchema`) ; liseré bleu quand le schéma est affiché. Le badge par-composant (`.part__internal-toggle` dans le corps) est supprimé du rendu. Validé headless (uno → bouton visible, toggle actif).
2. ℹ️ 7 segments 2/4 chiffres (pinInfo centré) : décision de GARDER tel quel (ne pas étaler pinInfo). Étaler casserait les schémas déjà câblés par les utilisateurs pour un gain purement cosmétique (câblage interne resserré au centre au lieu d'étalé sous les chiffres). Point clos.

# v2026.7.55
1. ✅ PIR : détection au SURVOL de la souris sur le composant EN SIMULATION (OUT=1 tant que la souris est au-dessus). Ctrl+clic = mouvement PERMANENT (bulle « Mouvement permanent (Ctrl+clic pour arrêter) »), Ctrl+clic à nouveau pour arrêter. Point rouge de détection affiché. Plus de propriété d'état. Handlers dans le shadowRoot du composant (survol + Ctrl+clic avec stopPropagation, actifs même en mode verrouillé) ; le moteur lit `el.motion` (relu à chaque frame via `updateMotion`, setInput au changement). Validé headless.

# v2026.7.54
1. ✅ DHT22 : broche de données renommée SDA → DATA (composant + `dht22Bindings` qui la résout). Deux curseurs EN SIMULATION (💧 humidité 0-100 %, 🌡 température -40 → +80 °C) à la place des propriétés d'inspecteur ; le moteur relit `el.humidity`/`el.temperature` en direct (liste DHT22 re-poussée au moteur à chaque `input`). Binding DATA→D7 et curseurs validés headless.

# v2026.7.53
1. ✅ Capteur de pouls actif : EN SIMULATION, un curseur règle le pouls (0-200 bpm). La sortie analogique OUT reproduit une courbe de pulsation cardiaque (forme PPG : pic systolique + onde dicrotique, deux gaussiennes), régénérée à chaque frame selon le BPM (sim.mts `updatePulses`/`pulseWaveform`, appelé dans `renderTick`). BPM=0 → ligne de base.
2. ✅ Capteur de température NTC actif : curseur -55 → +125 °C en simulation. Sortie analogique de type thermistance NTC (R0=10 k, B=3950, série 10 k) : la tension DIMINUE quand la température monte (diviseur Rntc/(Rntc+Rsérie), monotone décroissant vérifié : -55→0.99, 25→0.50, 125→0.035). Plus de propriété d'inspecteur (choix Frank). Pilotée en direct via l'événement `input`.

# v2026.7.52
1. ✅ Capteurs flamme / gaz / son / lumière ACTIFS en simulation (items « capteur de flamme… » + « pareil pour gaz/son/lumière ») : nouvelle base commune `AnalogDigitalSensorElement` (utils/) — double sortie AOUT (analogique) + DOUT (tout ou rien). EN SIMULATION, un curseur d'intensité (0-100 %, libellé Flamme/Gaz/Son/Lumière) ; propriété `sensitivity` (0-100 %) dans l'inspecteur = seuil DOUT. Convention (choix Frank) : AOUT baisse quand l'intensité monte ; DOUT actif-BAS (LOW) quand intensité > sensibilité. LED de détection allumée quand détecté.
2. ✅ Nouveau kind catalog `ao-do-sensor` + `aoDoSensorBindings(diagram)` (résout séparément AOUT et DOUT câblés) ; binding live dans sim.mts (curseur → setAnalog/setInput à chaque `input`). Réutilise l'infra simControl/simulating.
3. ✅ Photorésistance rebaptisée « Capteur de lumière » (label i18n « Light sensor » → « Capteur de lumière »). Test verify-diagram mis à jour (photorésistance testée en double sortie AO→A2, DO non câblé).

# v2026.7.51
1. ✅ Capteur d'inclinaison : propriété d'état retirée de l'inspecteur. EN SIMULATION (attribut `simulating`), un bouton « Incliner »/« Incliné » bascule l'état (tout ou rien) directement sur le composant ; le capteur s'incline visuellement (rotation -22°). Le moteur lit `el.tilted` en direct via l'événement `input` (binding dédié dans sim.mts, séparé des autres digital-source qui restent sur `attrs.state`). Réutilise l'infra simControl/simulating (v50). Validé Chrome headless.

# v2026.7.50
1. ✅ Capteur à ultrason : « HC-SR04 » retiré du nom (« Ultrasonic sensor » / « Capteur à ultrason »). Propriétés `distancemin`/`distancemax` dans l'inspecteur (défaut 2 cm → 400 cm) à la place de `distance`.
2. ✅ Curseur de simulation ajouté (infra `simControl` + attribut `simulating` posé par l'éditeur en mode verrouillé) : en simulation, un curseur `range` borné min→max + une zone de saisie `number` (+ « cm ») choisissent la distance mesurée. Hors simulation, aucun contrôle visible. Clamp min/max validé (999→50 sur plage 10-50).
3. ✅ Distance mise à jour EN DIRECT pendant la simulation : chaque objet sensor est muté sur l'événement `input` du curseur (même référence de tableau relue par le moteur à chaque impulsion TRIG). Distance de départ = milieu de la plage.
4. ℹ️ Infra `simControl`/`simulating` réutilisable pour les prochains items (tilt, flamme, gaz, son, lumière, pouls, température) qui demandent aussi un contrôle en simulation.

# v2026.7.49
1. ✅ Servo — bras single/double/cross rétablis (item « les bras ne changent plus ») : l'ancien palonnier figé unique (path49, un seul galet très long) est RETIRÉ du dessin retouché au montage et remplacé par des palonniers dessinés en procédural (renderHorn), pilotés par la propriété `horn` (déjà exposée par le catalog). single = 1 bras, double = 2 opposés, cross = croix 4 branches.
2. ✅ Servo — bras qui « sortait de la zone d'affichage en bas » corrigé : boîte agrandie en hauteur (viewBox 0 0 170 125, étendu vers le BAS seulement → broches y=60/70/80 inchangées) + bras raccourci (rayon 34 px). Plus aucun débordement à 0° ni 180°. Validé Chrome headless (3 formes × 5 angles). NB : le volet « très lent » de l'item reste à faire (besoin du programme de test).

# v2026.7.48
1. ✅ Câblage interne 7 segments trop grand en hauteur à l'affichage (alors que les SVG externe/interne se superposaient parfaitement dans Inkscape) : la surimpression `.part__internal` était étirée sur `.part__body` (offsetWidth/offsetHeight), or ce corps DOM est plus HAUT que le dessin (span d'étiquette sous le SVG) et le dessin externe garde son ratio (letterbox). Correction (editor.mts `renderInternalWiring`) : on mesure le SVG externe réel (`svg.width/height.baseVal`) + sa marge de centrage (getBoundingClientRect) et on cale l'overlay dessus (left/top/width/height posés en JS ; `.part__internal` passe de `inset:0` à `top/left:0`). L'interne coïncide maintenant au pixel avec l'externe.

# v2026.7.47
1. ✅ Buzzer : dessin repris du schéma validé de Frank (`svg retouche/Validé/buzzer.edit.svg`) — disque noir, dôme central en dégradé radial, 2 pattes (noire = broche 1, rouge = broche 2). Pastilles de positionnement retirées ; broches calées à x=20/30, y=50 (grille 10 px), conformes au pinInfo du fork. Rendu Chrome headless validé (note de musique flottante conservée).

# v2026.7.46
1. ✅ Câblage interne 7 segments : retour au SCALE SIMPLE (comme v41) — le calage 2D (v44/45) écrasait le schéma quand pinInfo est resserré. Le schéma de Frank est juste mis à l'échelle de la boîte (box/viewBox), ses broches sont déjà bien posées dans son repère. 2/4 chiffres à nouveau étalés sous les chiffres.

# v2026.7.45
1. ✅ Schémas internes 7 segments repartis des fichiers d'origine `.edit.svg` de Frank (ronds de positionnement + trait couleur corrigé sur le 2 chiffres). Pipeline pointé sur *.edit.svg → clean → flip anode ; bbox du tracé inchangées (calage 2D conservé). Les *.edit.svg sont désormais LA source à retoucher.

# v2026.7.44
1. ✅ Câblage interne 7 segments : calage 2D (X + Y) sur les broches réelles. La boîte des broches du tracé dessiné (SCHEMA_PIN_BBOX, mesurée par variante) est envoyée sur la boîte des pinInfo — les fils tombent EXACTEMENT sur les pastilles (1 chiffre : parfait). Corrige v43 (calage Y seul + bornes fausses → décalage). NB : sur 2/4 chiffres, pinInfo étant centré, le câblage se resserre au centre (cf. « à faire » n°8).

# v2026.7.43
1. ✅ Câblage interne 7 segments : calage vertical sur les broches réelles (les fils du haut n'atteignaient pas les pattes, diodes décalées vers le bas). La rangée de broches du schéma [SCHEMA_PIN_TOP=9.55, SCHEMA_PIN_BOT=79.95] est étirée sur les broches du composant [min,max Y de pinInfo] — comme le poster de brochage. Remplace la compression fixe de 2 px (v42) qui décalait.
2. ✅ 4 chiffres : gros trait noir parasite supprimé (path1 stroke-width 11.3 sans couleur = doublon Inkscape d'un fil fin). Filtre « stroke-width > 5 » ajouté à _clean-7seg-schema.mjs ; clean + anode régénérés.

# v2026.7.42
1. ✅ Dessin 7 segments : contour des segments retiré (setSeg strokeWidth 0.05 au lieu de 0.96 — segments pleins, sans liseré épais, comme le modèle fourni par Frank).
2. ✅ Afficheur 4 chiffres : points gris parasites (ex-pattes CLN/COM) retirés — bandes de pastilles haut/bas réduites (width 70→60, 6 pastilles au lieu de 7).
3. ✅ Schéma interne 7 segments compressé de 2 px en hauteur (centré), il était trop haut de 2 px vs le corps (SEVEN_SEG_SHRINK dans internal-wiring.mts).

# v2026.7.41
1. ✅ Câblage interne 7 segments : abandon du générateur procédural, on branche désormais les VRAIS schémas dessinés à la main par Frank (interne/7seg-*.schema.svg) — nettoyés en *.clean.svg (scripts/_clean-7seg-schema.mjs) puis posés à l'échelle du corps (repère = viewBox). Épaisseurs et couleurs d'origine (traits fins), échelle correcte (les broches tombent sur les pastilles). Corrige v40 (échelle et traits gros du 1dig, 2/4dig faux).
2. ✅ Diodes retournables cathode↔anode pour les 3 (1/2/4 chiffres) : variante anode générée par retournement de chaque diode (triangle + barre, rotation 180°) via scripts/_flip-7seg-diodes.mjs → *.anode.svg. `attrs.common` choisit clean (cathode) ou anode. 8/16/32 diodes retournées.

# v2026.7.40
1. ✅ Câblage interne 7 segments restylé (bouton ☢) : traits fins (0.6) au lieu du gros trait noir 2px, réseau du commun en bleu (comme les SVG dessinés à la main). Diodes retournées selon cathode/anode commune (attrs.common) — déjà géré par le helper `diode`.
2. ✅ 2/4 chiffres allégés : une diode par segment posée près de sa broche (orientée selon common) mais NON reliée au segment (câblage segment non dessiné = lisible) ; seules les broches communes DIG1…DIGn reliées par un bus bleu.
3. ✅ Broches CLN et COM retirées de l'afficheur 4 chiffres (VARIANTS[4].pins dans 7segment-element.mts).

# v2026.7.39
1. ✅ Posters de brochage (bouton ☢) pour Arduino Uno et Mega 2560 : les SVG pinout (rangées haut/bas + pastilles power/gnd) sont posés en surimpression, calés au pixel sur la carte comme le Pico (bornes rTop/rBot mesurées sur les pastilles, validation Chrome headless). Uno pile aligné ; mega calé haut/bas (bloc de ports latéraux affiché sous la carte).
2. ⏳ Nano : poster écarté — sa bande de broches (0.489→0.646) est trop resserrée face au ratio de la carte, l'étirement (k≈1.6) déborde. Le SVG nano-pinout doit être retouché avant activation (import + entrée POSTERS commentés dans pinout.mts).

# v2026.7.38
1. ✅ LED allumée plus lumineuse : halo passé PAR-DESSUS le corps (z-order — `#g30` déplacé après `#g33` dans led.svg) et agrandi/intensifié (rayons 13/3/4.5 au lieu de 10/2/3, opacité du groupe 1 au lieu de 0.85). La LED rayonne au lieu d'un simple point lumineux masqué derrière le plastique.

# v2026.7.37
1. ✅ Corrige le scintillement de l'afficheur 7 segments multiplexé (2/4 digits) en simulation Pico : le rendu ~60 Hz échantillonne le balayage MicroPython à un instant quasi aléatoire par rapport au cycle de scan simulé, révélant parfois un digit fraîchement éteint avant que le suivant ne soit rallumé. Anti-scintillement temporel dans sim.mts (`SEVEN_SEG_SETTLE_MS`) : un nouvel état de segment n'est publié que s'il est resté identique un court délai réel (40 ms), absorbant ce battement.
2. ✅ Support PWM des segments (1 chiffre) : un segment piloté en rapport cyclique (variateur de luminosité) utilise la mesure de duty cycle (`readPwmDuty`, comme la LED RGB) plutôt que le niveau instantané.

# v2026.7.36
1. ✅ Corrige le scintillement de l'afficheur 7 segments 1 chiffre en simulation Pico (tentative initiale, insuffisante — cf. v2026.7.37) : anti-scintillement `sevenSegStable` basé sur la stabilité de l'état lu sur 2 frames.

# v2026.7.35
1. ✅ Retour arrière complet de la console xterm.js (v2026.7.34) : console maison restaurée (sim.mts, panel.ts, styles.css), dépendances @xterm retirées — le terminal ne fonctionnait pas en réel, et le bug de collage venait du presse-papier (résolu côté système, pas côté code).
2. ℹ️ Conservés : import keypad-4col.schema.svg réparé et travaux SVG en cours.

# v2026.7.34
1. ✅ Console série / REPL remplacée par un vrai émulateur de terminal xterm.js (le même que le terminal VS Code) embarqué dans la webview : séquences ANSI, \r\n, effacement de ligne, collage multi-lignes et flèches (historique MicroPython) gérés nativement — corrige définitivement les sauts de ligne parasites au collage.
2. ✅ Micro-émulation maison supprimée (processAnsi, verrous contentEditable, handler paste) ; Ctrl+C avec sélection = copie, sans sélection = interruption (0x03) ; curseur visible seulement en mode REPL ; hors REPL clavier désactivé (la ligne d'envoi reste pour Arduino).
3. ✅ CSS xterm extrait par esbuild dans dist/webview.css (chargé par le panel, ajouté au vsix).
4. ✅ Smoke test Chrome headless : xterm monté dans #serial sans erreur JS ; verify:all OK.
5. ℹ️ Import keypad-schema.svg → keypad-4col.schema.svg (fichier renommé à la main, build cassé sinon).
