1. ⏳ J'ai retouché hall-interne met le nouveau. → **la retouche n'est pas dans `Composants2D.svg`** (réextraction identique au fichier déjà en place) : enregistrer la planche dans Inkscape, puis `node scripts/_extract-composants.mjs TO92S hall-interne@TO92S` + `git checkout src/webview/composants/externe/TO92S.svg`.
1. ✅ Nouvelle résistance verticale et son schma interne. A intégrer via une propsiété (verticale ou horizontale) de la résistance
1. 
