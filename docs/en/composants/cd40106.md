# CD40106 — hex Schmitt-trigger inverter

![CD40106](../../img/composants/cd40106.webp)

DIL-14 package for the breadboard: **six independent inverters**. An inverter outputs the **opposite** of its input — 1 becomes 0, 0 becomes 1.

CMOS 4000 series: it runs on **3 to 18 V**, the widest range of the lot — as happy with the 5 V of an Uno as with the 3.3 V of a Pico.

These inverters are **Schmitt-trigger** ones: on real hardware their switching threshold rises on the way up and drops on the way down, which cleans up a slow or noisy signal. Levels are clean in simulation, so the hysteresis never shows.

## Pins

| Pin | Name | Role |
|---|---|---|
| 1 | **a** | input of inverter A |
| 2 | **a̅** | output of inverter A (bar = inverted) |
| 3 | **b** | input of inverter B |
| 4 | **b̅** | output of inverter B (bar = inverted) |
| 5 | **c** | input of inverter C |
| 6 | **c̅** | output of inverter C (bar = inverted) |
| 7 | **GND** | ground — **black wire** |
| 8 | **d̅** | output of inverter D (bar = inverted) |
| 9 | **d** | input of inverter D |
| 10 | **e̅** | output of inverter E (bar = inverted) |
| 11 | **e** | input of inverter E |
| 12 | **f̅** | output of inverter F (bar = inverted) |
| 13 | **f** | input of inverter F |
| 14 | **VDD** | supply — **red wire** |

## Truth table

One inverter; the other five are identical.

| a | a̅ |
|---|---|
| 0 | 1 |
| 1 | 0 |

## Properties

| Property | Role | Default |
|---|---|---|
| `ref` | Part number — changing it changes the pinout, not the drawing | CD40106 |

## Simulation

- **Power is not optional**: without a red wire on VDD (pin 14) and a black wire on GND (pin 7), the chip outputs nothing at all.
- Outside the **3 to 18 V** range Kablix reports “Incompatible supply voltage”. **Below it** the chip does nothing; **above it** the chip is DESTROYED — red cross, and it stays that way until the next run.
- A **floating input is not a 0**: the output stays undefined as long as that input matters — and for this function it always matters.
- An output drives the circuit just like a board pin: an LED **with its resistor**, a transistor base, or the input of another chip.

## Usage

- An inverter turns a logic level into its opposite; two in a row leave the level alone but **clean the signal up**.
- The 6 inverters in the package are independent: one chip can serve 6 unrelated purposes.
- Wire the inputs to board outputs and the output to a board input: the program then reads a result computed by the chip, not by the microcontroller.
- Ready-made test: `CI-uno` and `CI-pico` in `testkablix/` — the eleven chips wired together, every gate checked.

---

*Kablix in-house component — drawing by Frank Sauret.*
