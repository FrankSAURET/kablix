diff --git a/node_modules/rp2040js/dist/esm/clock/simulation-clock.js b/node_modules/rp2040js/dist/esm/clock/simulation-clock.js
index 3b9a7ac..6219177 100644
--- a/node_modules/rp2040js/dist/esm/clock/simulation-clock.js
+++ b/node_modules/rp2040js/dist/esm/clock/simulation-clock.js
@@ -36,7 +36,14 @@ export class SimulationClock {
         alarm.nanos = this.nanos + nanos;
         let alarmListItem = this.nextAlarm;
         let lastItem = null;
-        while (alarmListItem && alarmListItem.nanos < alarm.nanos) {
+        // KABLIX PATCH: insert same-deadline alarms in FIFO order (<=) instead
+        // of LIFO (<). With LIFO, a DMA TX channel rescheduling itself at the
+        // current instant kept cutting in front of the pending RX channel alarm,
+        // starving it until the SPI RX FIFO overflowed — the RX channel then
+        // never reached transCount 0 and the firmware hung on
+        // dma_channel_is_busy() (MicroPython machine.SPI writes >= 32 bytes).
+        // FIFO also matches the RP2040's round-robin DMA arbitration.
+        while (alarmListItem && alarmListItem.nanos <= alarm.nanos) {
             lastItem = alarmListItem;
             alarmListItem = alarmListItem.next;
         }
diff --git a/node_modules/rp2040js/dist/esm/cortex-m0-core.js b/node_modules/rp2040js/dist/esm/cortex-m0-core.js
index d8d31ae..d9b35b8 100644
--- a/node_modules/rp2040js/dist/esm/cortex-m0-core.js
+++ b/node_modules/rp2040js/dist/esm/cortex-m0-core.js
@@ -522,9 +522,24 @@ export class CortexM0Core {
         }
         // ARM Thumb instruction encoding - 16 bits / 2 bytes
         const opcodePC = this.PC & ~1; //ensure no LSB set PC are executed
-        const opcode = this.readUint16(opcodePC);
+        // KABLIX PATCH: fetch straight from the flash view. Every instruction
+        // used to go through core.readUint16 -> rp2040.readUint16 -> range tests
+        // -> getUint16: two calls and two comparisons, on the single hottest path
+        // of the emulator (10 % of the profile across its two sites). Code runs
+        // from XIP flash, so that case is inlined here and everything else —
+        // RAM, bootrom, a negative PC — falls back to the original path with
+        // identical semantics (same window as rp2040.readUint16: the first flash
+        // image, not the mirrors).
+        const rp = this.rp2040;
+        const flashOffset = opcodePC - 0x10000000;
+        const inFlash = flashOffset >= 0 && flashOffset < rp.flash.length;
+        const opcode = inFlash ? rp.flashView.getUint16(flashOffset, true) : this.readUint16(opcodePC);
         const wideInstruction = opcode >> 12 === 0b1111 || opcode >> 11 === 0b11101;
-        const opcode2 = wideInstruction ? this.readUint16(opcodePC + 2) : 0;
+        const opcode2 = wideInstruction
+            ? (inFlash && flashOffset + 2 < rp.flash.length
+                ? rp.flashView.getUint16(flashOffset + 2, true)
+                : this.readUint16(opcodePC + 2))
+            : 0;
         this.PC += 2;
         let deltaCycles = 1;
         // ADCS
diff --git a/node_modules/rp2040js/dist/esm/peripherals/pio.d.ts b/node_modules/rp2040js/dist/esm/peripherals/pio.d.ts
index 2aea8c1..ecd36dc 100644
--- a/node_modules/rp2040js/dist/esm/peripherals/pio.d.ts
+++ b/node_modules/rp2040js/dist/esm/peripherals/pio.d.ts
@@ -71,6 +71,8 @@ export declare class StateMachine {
     wait(type: WaitType, polarity: boolean, index: number): void;
     nextPC(): void;
     step(): void;
+    clkDivAccum: number;
+    advance(sysCycles: number): void;
     setSetPinDirs(value: number): void;
     setSetPins(value: number): void;
     setSideset(value: number, count: number): void;
@@ -117,6 +119,7 @@ export declare class RPPIO extends BasePeripheral implements Peripheral {
     irqUpdated(): void;
     checkChangedPins(): void;
     step(): void;
+    advance(sysCycles: number): void;
     run(): void;
     stop(): void;
 }
diff --git a/node_modules/rp2040js/dist/esm/peripherals/pio.js b/node_modules/rp2040js/dist/esm/peripherals/pio.js
index f87e56c..85a7f48 100644
--- a/node_modules/rp2040js/dist/esm/peripherals/pio.js
+++ b/node_modules/rp2040js/dist/esm/peripherals/pio.js
@@ -127,6 +127,10 @@ export class StateMachine {
         this.updatePC = true;
         this.clockDivInt = 1;
         this.clockDivFrac = 0;
+        // KABLIX PATCH: fractional clock-divider accumulator (256ths of a
+        // system-clock cycle), driven by advance(sysCycles) instead of the
+        // unimplemented clkDivRestart() + free-running setTimeout loop.
+        this.clkDivAccum = 0;
         this.execCtrl = 0x1f << 12;
         this.shiftCtrl = 0b11 << 18;
         this.pinCtrl = 0x5 << 26;
@@ -629,6 +633,25 @@ export class StateMachine {
             this.nextPC();
         }
     }
+    // KABLIX PATCH: honor CLKDIV (SMx_CLKDIV, unused upstream — clkDivRestart()
+    // was a no-op stub) by only running an SM cycle once the fractional
+    // system-clock accumulator crosses the configured divisor. divisor 0 means
+    // 65536 per the RP2040 datasheet. Pins are checked after EACH individual
+    // step (not just once at the end of the batch) — a single advance() call
+    // can cross several SM cycles, and a fast toggle (WS2812 bit ≈ a handful
+    // of SM cycles) must not be collapsed to its final state only.
+    advance(sysCycles) {
+        if (!this.enabled) return;
+        const divisor = this.clockDivInt === 0 && this.clockDivFrac === 0
+            ? 65536
+            : this.clockDivInt + this.clockDivFrac / 256;
+        this.clkDivAccum += sysCycles;
+        while (this.clkDivAccum >= divisor) {
+            this.clkDivAccum -= divisor;
+            this.step();
+            this.pio.checkChangedPins();
+        }
+    }
     setSetPinDirs(value) {
         this.pio.pinDirectionsChanged(value, this.setBase, this.setCount);
     }
@@ -757,7 +780,7 @@ export class StateMachine {
         // TODO any pin write left asserted due to OUT_STICKY.
     }
     clkDivRestart() {
-        this.pio.warn('clkDivRestart not implemented');
+        this.clkDivAccum = 0;
     }
     checkWait() {
         if (!this.waiting) {
@@ -972,7 +995,8 @@ export class RPPIO extends BasePeripheral {
                 const shouldRun = value & 0xf;
                 if (this.stopped && shouldRun) {
                     this.stopped = false;
-                    this.run();
+                    // KABLIX PATCH: no self-scheduling run() — advance() is
+                    // driven by the host (pico.mts) in step with CPU cycles.
                 }
                 if (!shouldRun) {
                     this.stopped = true;
@@ -1069,6 +1093,19 @@ export class RPPIO extends BasePeripheral {
         }
         this.checkChangedPins();
     }
+    // KABLIX PATCH: system-clock-driven replacement for the free-running
+    // setTimeout(0) loop in run() below, which executes PIO instructions at
+    // browser wall-clock speed instead of the emulated CPU's simulated time
+    // (drifts arbitrarily from the CPU in step/fast-forward mode, and ignores
+    // CLKDIV entirely). Call once per CPU instruction with the cycle count it
+    // consumed; each SM advances at its own configured clock divisor.
+    advance(sysCycles) {
+        if (this.stopped) return;
+        for (const machine of this.machines) {
+            machine.advance(sysCycles);
+        }
+        this.checkChangedPins();
+    }
     run() {
         for (let i = 0; i < 1000 && !this.stopped; i++) {
             this.step();
diff --git a/node_modules/rp2040js/dist/esm/rp2040.js b/node_modules/rp2040js/dist/esm/rp2040.js
index 2a29f91..98ea905 100644
--- a/node_modules/rp2040js/dist/esm/rp2040.js
+++ b/node_modules/rp2040js/dist/esm/rp2040.js
@@ -182,6 +182,14 @@ export class RP2040 {
             this.logger.error(LOG_NAME, `read from address ${address.toString(16)}, which is not 32 bit aligned`);
         }
         const { bootrom } = this;
+        // KABLIX PATCH: SRAM first. The four ranges below are disjoint, so the
+        // order is a pure performance choice — and MicroPython reads SRAM far
+        // more often than the bootrom, which upstream tests first. The upstream
+        // SRAM branch further down is now unreachable; it is left in place so
+        // this patch stays a pure insertion, easy to rebase.
+        if (address >= RAM_START_ADDRESS && address < RAM_START_ADDRESS + this.sram.length) {
+            return this.sramView.getUint32(address - RAM_START_ADDRESS, true);
+        }
         if (address < bootrom.length * 4) {
             return bootrom[address / 4];
         }
@@ -215,6 +223,15 @@ export class RP2040 {
         return 0xffffffff;
     }
     findPeripheral(address) {
+        // KABLIX PATCH: range test before the dictionary lookup. `peripherals` is
+        // a plain object keyed by large, sparse integers, so V8 stores it in
+        // dictionary mode and every lookup hashes. Every RAM write used to pay
+        // for one missed hash (writeUint32 asks here first). Peripherals only
+        // live at 0x18000000 (XIP_SSI) and 0x40000000..0x50400000, so a single
+        // comparison rules out bootrom, flash and SRAM.
+        if (address < 0x40000000) {
+            return address >>> 14 === 0x6000 ? this.peripherals[0x18000] : undefined;
+        }
         return this.peripherals[(address >>> 14) << 2];
     }
     /** We assume the address is 16-bit aligned */
@@ -241,6 +258,14 @@ export class RP2040 {
     writeUint32(address, value) {
         address = address >>> 0;
         const { bootrom } = this;
+        // KABLIX PATCH: SRAM first, before asking findPeripheral. No peripheral
+        // key maps into the SRAM window (they sit at 0x18000000 and
+        // 0x40000000+), so this only reorders disjoint ranges — but it takes the
+        // most frequent write in the whole emulator off the peripheral path.
+        if (address >= RAM_START_ADDRESS && address < RAM_START_ADDRESS + this.sram.length) {
+            this.sramView.setUint32(address - RAM_START_ADDRESS, value, true);
+            return;
+        }
         const peripheral = this.findPeripheral(address);
         if (peripheral) {
             const atomicType = (address & 0x3000) >> 12;
